---
alwaysApply: false
description: 基于 background 各 app 的 Django models 扫描生成的完整数据库文档（表清单、字段要点、实体关系）。
---

# 投资社区后端数据库文档（最终完整版）

> **生成依据**：扫描 `background/` 下各业务应用的 `models.py`（`accounts`、`content`、`market_data`、`portfolios`、`notifications`、`reports`、`groups`、`messages`），以 **`Meta.db_table`** 为物理表名基准。  
> **技术栈**：Django 4.x ORM、MySQL 8.x（建议 InnoDB + utf8mb4）、自定义用户模型 `accounts.User`（表 `user`）。

---

## 一、全局约定

### 1.1 与用户模型

- 外键指向用户时，ORM 多为 `ForeignKey(accounts.User)`，物理列名为 `user_id` 或按字段名 `author_id`、`owner_id`、`reporter_id` 等。
- 密码与权限：`AbstractBaseUser` + `PermissionsMixin`，密码字段为 `password`（非 `password_hash`）。

### 1.2 枚举与旧版文档的差异说明

- 早期设计文档中的 **TINYINT 状态码** 与当前代码 **不一致**；本库大量使用 **字符串枚举**（如 `User.status`: `NORMAL` / `MUTED` / `BANNED`，`Content.status`: `DRAFT` / `PUBLISHED` 等），以代码为准。
- 物理表名与旧文档可能一致（如 `content`、`asset`），但字段类型与选项以 **Django 模型** 为准。

### 1.3 标的资产「真实 code」原则（代码注释与实现一致）

| 层级 | 字段 | 说明 |
| --- | --- | --- |
| 展示/输入 | `asset.code` + `asset.market` | 如 `600519` + `SH` |
| 第三方 Finnhub | `asset.finnhub_symbol` | 调 API 用，唯一（允许 NULL） |
| 业务关联 | `asset_id` | `content_asset`、`portfolio_asset`、`user_holding` 等 |

---

## 二、表清单总览（按业务域）

### 2.1 用户与账号（`accounts`）

| 物理表名 | Django 模型 | 说明 |
| --- | --- | --- |
| `user` | `User` | 用户主表 |
| `user_invest_profile` | `UserInvestProfile` | 投资偏好（1:1） |
| `user_follow` | `UserFollow` | 关注关系 |
| `user_star_follow` | `UserStarFollow` | 特别关注（星标） |
| `follow_feed_item` | `FollowFeedItem` | 关注动态预写入 |
| `user_privacy_settings` | `UserPrivacySettings` | 隐私设置（1:1） |
| `user_moderation_log` | `UserModerationLog` | 用户治理操作日志 |
| `user_social_account` | `UserSocialAccount` | 第三方账号绑定 |
| `level_rule` | `LevelRule` | 积分等级规则 |
| `user_point_log` | `UserPointLog` | 积分流水 |
| `user_behavior_daily` | `UserBehaviorDaily` | 用户行为日聚合 |
| `user_verification_code` | `UserVerificationCode` | 验证码 |
| `user_real_name_verification` | `UserRealNameVerification` | 实名认证申请 |
| `user_professional_verification` | `UserProfessionalVerification` | 专业认证申请 |
| `risk_questionnaire_template` | `RiskQuestionnaireTemplate` | 风险问卷模板 |
| `risk_questionnaire_submission` | `RiskQuestionnaireSubmission` | 风险问卷提交 |

### 2.2 社区内容与标的（`content`）

| 物理表名 | Django 模型 | 说明 |
| --- | --- | --- |
| `asset` | `Asset` | 标的资产库 |
| `board` | `Board` | 社区板块（树形，最多三级） |
| `content` | `Content` | 帖子/文章 |
| `content_asset` | `ContentAsset` | 内容 ↔ 标的（M2M 中间表） |
| `content_board` | `ContentBoard` | 内容 ↔ 板块（M2M 中间表） |
| `content_meta` | `ContentMeta` | 内容扩展（类型、格式、转发统计等，1:1） |
| `poll` / `poll_option` / `poll_vote` | `Poll` / `PollOption` / `PollVote` | 投票 |
| `repost` | `Repost` | 转发 |
| `mention` | `Mention` | @提及 |
| `content_attachment` | `ContentAttachment` | 帖子附件 |
| `comment_attachment` | `CommentAttachment` | 评论附件 |
| `comment` | `Comment` | 评论（楼中楼） |
| `like` | `Like` | 点赞（帖子/评论/组合） |
| `favorite` | `Favorite` | 收藏帖子 |

### 2.3 行情与同步任务（`market_data`）

| 物理表名 | Django 模型 | 说明 |
| --- | --- | --- |
| `asset_quote_snapshot` | `AssetQuoteSnapshot` | 行情快照（FK → `content.Asset`） |
| `asset_kline` | `AssetKline` | K 线（FK → `content.Asset`） |
| `data_job_log` | `DataJobLog` | 同步任务日志 |

### 2.4 组合与持仓（`portfolios`）

| 物理表名 | Django 模型 | 说明 |
| --- | --- | --- |
| `portfolio` | `Portfolio` | 投资组合 |
| `portfolio_asset` | `PortfolioAsset` | 组合持仓（FK → `Asset`，可空兼容历史） |
| `user_holding` | `UserHolding` | 个人持仓 |
| `holding_daily_snapshot` | `HoldingDailySnapshot` | 持仓日估值快照 |
| `portfolio_comment` | `PortfolioComment` | 组合评论 |
| `portfolio_subscription` | `PortfolioSubscription` | 组合订阅 |
| `portfolio_favorite` | `PortfolioFavorite` | 组合收藏 |
| `portfolio_update_log` | `PortfolioUpdateLog` | 组合更新说明 |

### 2.5 通知（`notifications`）

| 物理表名 | Django 模型 | 说明 |
| --- | --- | --- |
| `notification` | `Notification` | 站内通知 |

### 2.6 治理、举报与统计（`reports`）

| 物理表名 | Django 模型 | 说明 |
| --- | --- | --- |
| `report` | `Report` | 举报 |
| `alert` | `Alert` | 风控告警 |
| `moderation_rule` | `ModerationRule` | 审核规则 |
| `moderation_queue_item` | `ModerationQueueItem` | 审核队列（FK → `Content`） |
| `moderation_hit` | `ModerationHit` | 规则命中日志 |
| `community_metric_daily` | `CommunityMetricDaily` | 社区日指标 |
| `topic_metric_daily` | `TopicMetricDaily` | 话题日指标 |

### 2.7 群组（`groups`）

| 物理表名 | Django 模型 | 说明 |
| --- | --- | --- |
| `groups` | `Group` | 群组 |
| `group_members` | `GroupMember` | 群成员 |
| `group_join_requests` | `GroupJoinRequest` | 入群申请 |
| `group_reviewers` | `GroupReviewer` | 群审核人 |
| `group_invites` | `GroupInvite` | 群邀请 |
| `group_posts` | `GroupPost` | 群讨论帖 |
| `group_files` | `GroupFile` | 群资料文件 |

### 2.8 私信（`messages`）

| 物理表名 | Django 模型 | 说明 |
| --- | --- | --- |
| `message_conversation` | `Conversation` | 会话 |
| `message_conversation_participant` | `ConversationParticipant` | 会话参与者 |
| `message` | `Message` | 消息正文 |
| `message_read_log` | `MessageReadLog` | 消息已读 |
| `message_attachment` | `MessageAttachment` | 私信附件 |

---

## 三、核心实体关系（ER）

### 3.1 关系总览图（Mermaid）

下列关系为 **逻辑关系**；具体 ON DELETE 行为以模型 `on_delete` 为准（`CASCADE` / `PROTECT` / `SET_NULL` 等）。

```mermaid
erDiagram
    user ||--o{ content : "author"
    user ||--o{ comment : "author"
    user ||--o{ portfolio : "owner"
    user ||--o{ user_holding : "holds"
    user ||--o{ like : "likes"
    user ||--o{ favorite : "favorites"
    user ||--o{ notification : "receives"

    asset ||--o{ content_asset : "tagged_in"
    content ||--o{ content_asset : "has"
    content ||--o{ comment : "has"
    content ||--o{ favorite : "has"
    content ||--o{ like : "POST target"

    asset ||--o{ asset_quote_snapshot : "quotes"
    asset ||--o{ asset_kline : "klines"
    asset ||--o{ portfolio_asset : "in_portfolio"
    asset ||--o{ user_holding : "held_by"

    portfolio ||--o{ portfolio_asset : "contains"
    portfolio ||--o{ portfolio_comment : "has"
    portfolio ||--o{ portfolio_subscription : "subscribed"
    portfolio ||--o{ portfolio_favorite : "favorited"

    user_holding ||--o{ holding_daily_snapshot : "daily_snapshots"

    content ||--o| content_meta : "meta"
    content ||--o| poll : "poll"
    poll ||--o{ poll_option : "options"
    poll ||--o{ poll_vote : "votes"

    board ||--o{ board : "parent_child"
    content }o--o{ board : "content_board"

    groups ||--o{ group_members : "members"
    groups ||--o{ group_posts : "posts"
    user ||--o{ groups : "owns"

    message_conversation ||--o{ message_conversation_participant : "participants"
    message_conversation ||--o{ message : "messages"

    user ||--|| user_invest_profile : "1:1"
    user ||--|| user_privacy_settings : "1:1"
```

### 3.2 关键关系说明（文字）

**用户 `user`**

- **1:1**：`user_invest_profile`、`user_privacy_settings`。
- **1:N**：发布的 `content`、发表的 `comment`、创建的 `portfolio`、点赞 `like`、收藏 `favorite`、通知 `notification`、举报 `report`、私信 `message` 等。
- **M:N 关注**：`user_follow`（`follower` ↔ `followee`）、`user_star_follow`（星标）。

**标的 `asset`（content 应用）**

- **被引用**：`content_asset`（帖子关联标的）、`portfolio_asset`（`PROTECT`）、`user_holding`（`PROTECT`）。
- **行情子表**（`market_data` 应用，外键指向 `content.Asset`）：`asset_quote_snapshot`、`asset_kline`。

**内容 `content`**

- **M:N**：通过 `content_asset` 关联 `asset`；通过 `content_board` 关联 `board`。
- **1:1**：`content_meta`、可选 `poll`（投票帖）。
- **1:N**：`comment`、`favorite`、`repost`、`content_attachment`、`moderation_queue_item`（审核队列）、`moderation_hit`。

**点赞 `like`**

- `target_type`：`POST` / `COMMENT` / `PORTFOLIO`；`target_id` 为对应表主键（多态关联，非数据库级 FK）。

**组合 `portfolio`**

- **1:N**：`portfolio_asset`、`portfolio_comment`、`portfolio_subscription`、`portfolio_favorite`、`portfolio_update_log`。
- **点赞**：与帖子一样走 `like` 表，`target_type=PORTFOLIO`。

**持仓 `user_holding`**

- **1:N**：`holding_daily_snapshot`（按日收盘价估值，与 `AssetKline` 日 K 配合计算收益）。

**举报 `report`**

- 可选关联 `moderation_queue_item`（`linked_queue_item_id`）。
- `target_type`：帖子/评论/用户/组合等多态，`target_id` 无联合 FK。

**群组 `groups`**

- `Group` 1:N `GroupMember`、`GroupPost`、`GroupFile`；入群流程 `group_join_requests`、`group_invites`；`group_reviewers` 记录群级审核人。

**私信**

- `message_conversation` ↔ `message_conversation_participant`（用户与会话 M:N）；`message` 归属会话；`message_read_log` 记录已读；`message_attachment` 挂消息。

---

## 四、跨模块外键速查表

| 子表（物理表名） | 指向 | 说明 |
| --- | --- | --- |
| `asset_quote_snapshot` | `asset.id` | `asset_id` |
| `asset_kline` | `asset.id` | `asset_id` |
| `content` | `user.id` | `author_id`；`reviewed_by_id` 可选 |
| `content_asset` | `content.id`, `asset.id` | 中间表，唯一 `(content_id, asset_id)` |
| `content_board` | `content.id`, `board.id` | 中间表 |
| `content_meta` | `content.id` | 1:1 |
| `poll` | `content.id` | 1:1 |
| `poll_option` | `poll.id` | N:1 |
| `poll_vote` | `poll.id`, `poll_option.id`, `user.id` | 唯一三元组防重复投票 |
| `comment` | `content.id`, `user.id` | `parent_id` 自关联；`reply_to_user_id` |
| `favorite` | `user.id`, `content.id` | |
| `portfolio` | `user.id` | `owner_id` |
| `portfolio_asset` | `portfolio.id`, `asset.id`（可空） | `unique_together`: portfolio + asset |
| `user_holding` | `user.id`, `asset.id` | `unique_together`: user + asset |
| `holding_daily_snapshot` | `user_holding.id` | `unique_together`: holding + date |
| `report` | `user.id` | `reporter_id`；`handled_by_id`；`linked_queue_item_id` → `moderation_queue_item` |
| `moderation_queue_item` | `content.id` | `decided_by_id` → user |
| `moderation_hit` | `content.id`, `user.id` | `rule_id` 可空 |
| `notification` | `user.id` | 接收者 |
| `groups` | `user.id` | `owner_id` |
| `group_members` | `groups.id`, `user.id` | |
| `group_posts` | `groups.id`, `user.id` | `author_id` |
| `message_conversation_participant` | `message_conversation.id`, `user.id` | |
| `message` | `message_conversation.id`, `user.id` | `sender_id` |
| `message_read_log` | `message.id`, `user.id` | |

---

## 五、各表字段要点（按域摘录）

> 下列为 **设计与联调要点**；完整字段名、类型、默认值以各 `models.py` 为准。

### 5.1 `user`（`accounts.User`）

- 认证：`username`（唯一）、`email`（唯一）、`password`（哈希）。
- 资料：`display_name`、`avatar_url`、`bio`、`phone`（可空）、`investment_experience`。
- 角色/状态：`role`（`USER`/`MODERATOR`/`ADMIN`）、`status`（`NORMAL`/`MUTED`/`BANNED`）、`mute_until`。
- 社区数值：`followers_count`、`following_count`、`points`、`level`、`quality_score`。
- 认证扩展：`phone_verified`、`email_verified`、`identity_level`、`real_name_status`、`professional_status`、`risk_assessment_status`、`risk_level`、`v_badge`。
- Django：`is_active`、`is_staff`、`is_superuser`；`created_at`、`updated_at`。

### 5.2 `asset`（`content.Asset`）

- 业务键：`unique_together` = (`asset_type`, `code`, `market`)。
- Finnhub：`finnhub_symbol` 唯一（可空）；`exchange`、`currency`、`last_sync_at`；扩展 `meta_json`。
- 其它：`isin`、`industry`、`logo_url`、`description` 等。

### 5.3 `content`（`content.Content`）

- 正文：`title`、`body`、`tags_json`。
- 审核：`status`、`reviewed_by`、`reject_reason`、`risk_score`、`risk_level`、`moderation_source`。
- 计数：`like_count`、`comment_count`、`view_count`；`published_at`。

### 5.4 `like`（`content.Like`）

- `unique_together`：`user` + `target_type` + `target_id`。
- `target_type`：`POST` | `COMMENT` | `PORTFOLIO`（对应 `content` / `comment` / `portfolio` 的主键）。

### 5.5 `asset_quote_snapshot` / `asset_kline`（`market_data`）

- 外键：`asset` → `content.Asset`。
- K 线：`unique_together` = (`asset`, `resolution`, `k_time`)；索引支持按资产+周期拉时序。

### 5.6 `report`（`reports.Report`）

- `target_type`：`POST` | `COMMENT` | `USER` | `PORTFOLIO`。
- 处理：`status`、`handled_by`、`handle_result`、`handle_time`；可关联 `moderation_queue_item`。

### 5.7 群组表前缀

- 物理表名：`groups`、`group_members` 等（`Group` 模型对应表 **`groups`**，注意与 SQL 关键字冲突时 ORM 已显式指定 `db_table`）。

---

## 六、与历史文档（`数据库-合并版.mdc` / `数据库.mdc`）的对照

| 项目 | 旧合并文档 | 当前代码 |
| --- | --- | --- |
| 用户主键字段名 | 常写作 `password_hash` | Django：`password` |
| 内容状态 | 数字 0–5 | 字符串：`DRAFT`、`PUBLISHED` 等 |
| 点赞表名 | `likes` | **`like`** |
| 收藏 | 部分描述含 `favorite_count` 在 content 上 | 当前 `content` **无** `favorite_count` 字段；收藏见 `favorite` 表 |
| 组合持仓 | 仅 `symbol` 唯一 | 现为 **`portfolio` + `asset`（FK）** 唯一，`symbol`/`name` 为冗余 |
| 扩展表 | 文档未列全 | 实际含 **私信、群组、投票、审核队列、持仓快照、积分规则** 等 |

开发、写论文、导出 ER 图时，**以本文件 + 仓库内 `models.py` 为准**。

---

## 七、典型查询路径（与实现一致）

1. **标的讨论区**：`content_asset` → `content`（`status=PUBLISHED`）按 `published_at` 排序。  
2. **最新行情**：`asset_quote_snapshot` 按 `asset_id` + `quote_time` 降序取 1 条。  
3. **K 线**：`asset_kline` 按 `asset` + `resolution` + `k_time` 范围/倒序。  
4. **组合点赞数**：`like` 中 `target_type=PORTFOLIO` 且 `target_id=portfolio.id`（与 `portfolio.like_count` 冗余字段需业务层同步）。  
5. **持仓收益**：`user_holding` × `holding_daily_snapshot` × `asset_kline`（日 K 收盘价）。

---

## 八、统计

| 项 | 数量 |
| --- | --- |
| 物理表（`db_table`）总数 | **64**（以当前 models 中 `db_table` 计数） |
| Django 应用（含模型） | 8 |

---

*文档随模型变更请同步更新；新增模型时在本文件「表清单」与「关系」两节追加即可。*
