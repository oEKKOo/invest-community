# 投研社区完整API接口文档

## 📖 文档说明

本文档是**投研社区**后端API的完整接口规范，包含所有接口的详细说明、请求示例、响应格式和测试指南。

### 🏗️ 系统架构
- **后端**: Django + Django REST Framework
- **数据库**: MySQL 8.x
- **认证**: JWT Token
- **API风格**: RESTful API

### 🌐 基础配置
- **Base URL**: `http://127.0.0.1:8000`
- **API前缀**: `/api/`
- **响应格式**: JSON
- **字符编码**: UTF-8

---

## 🔑 认证说明

### JWT Token使用
大部分接口需要在请求头中携带认证token：
```
Authorization: Bearer {access_token}
```

### Token获取流程
1. 用户注册或登录 → 获取access_token和refresh_token
2. 使用access_token访问需要认证的接口
3. Token过期时使用refresh_token刷新

---

## 📊 通用响应格式

### 成功响应
```json
{
  "code": 0,
  "data": {
    // 具体数据内容
  }
}
```

### 错误响应
```json
{
  "code": 4001,
  "message": "错误描述",
  "errors": {
    // 具体错误详情（可选）
  }
}
```

### 错误码说明
| Code | 说明 |
|------|------|
| 0 | 成功 |
| 4001 | 参数校验失败 |
| 4010 | 未登录/Token无效 |
| 4030 | 无权限 |
| 4040 | 资源不存在 |
| 4090 | 重复操作 |
| 5000 | 服务端异常 |

---

# 📋 API接口清单

## 1. 认证模块 (Authentication)

### 1.1 用户注册
- **接口**: `POST /api/auth/register/`
- **权限**: 游客可访问
- **描述**: 用户注册新账号

#### 请求参数
```json
{
  "username": "testuser",
  "email": "test@example.com",
  "password": "12345678",
  "password_confirm": "12345678",
  "phone": "13888888888"
}
```

#### 参数说明
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | string | ✅ | 用户名，3-150字符，唯一 |
| email | string | ✅ | 邮箱地址，唯一 |
| password | string | ✅ | 密码，至少8位 |
| password_confirm | string | ✅ | 确认密码，需与password一致 |
| phone | string | ❌ | 手机号 |

#### 成功响应 (201)
```json
{
  "code": 0,
  "data": {
    "id": 1,
    "username": "testuser",
    "displayName": "testuser",
    "role": "USER",
    "access": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refresh": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

#### 错误响应示例
```json
{
  "code": 4001,
  "message": "注册失败",
  "errors": {
    "username": ["具有 用户名 的 用户 已存在。"],
    "email": ["具有 邮箱 的 用户 已存在。"]
  }
}
```

### 1.2 用户登录
- **接口**: `POST /api/auth/login/`
- **权限**: 游客可访问
- **描述**: 用户登录获取Token

#### 请求参数
```json
{
  "username": "testuser",
  "password": "12345678"
}
```

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "access": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refresh": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "username": "testuser",
      "displayName": "testuser",
      "role": "USER"
    }
  }
}
```

### 1.3 刷新Token
- **接口**: `POST /api/auth/refresh/`
- **权限**: 需要refresh_token
- **描述**: 刷新访问令牌

#### 请求参数
```json
{
  "refresh": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

#### 成功响应 (200)
```json
{
  "access": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

### 1.4 用户登出
- **接口**: `POST /api/auth/logout/`
- **权限**: 需要登录
- **描述**: 用户登出（可选实现）

#### 请求头
```
Authorization: Bearer {access_token}
```

#### 成功响应 (200)
```json
{
  "code": 0,
  "message": "登出成功"
}
```

---

## 2. 用户管理 (Users)

### 2.1 获取当前用户信息
- **接口**: `GET /api/users/me/`
- **权限**: 需要登录
- **描述**: 获取当前登录用户详细信息

#### 请求头
```
Authorization: Bearer {access_token}
```

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "id": 1,
    "username": "testuser",
    "displayName": "Test User",
    "avatar": "https://example.com/avatar.jpg",
    "role": "USER",
    "bio": "我的个人简介",
    "followers": 12,
    "following": 5,
    "created_at": "2026-01-29T10:00:00Z"
  }
}
```

### 2.2 获取用户主页
- **接口**: `GET /api/users/{user_id}/`
- **权限**: 游客可访问
- **描述**: 获取指定用户的公开信息

#### 路径参数
- `user_id`: 用户ID

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "id": 1,
    "username": "testuser",
    "displayName": "Test User",
    "avatar": "https://example.com/avatar.jpg",
    "bio": "我的个人简介",
    "followers": 12,
    "following": 5,
    "created_at": "2026-01-29T10:00:00Z"
  }
}
```

### 2.3 更新用户资料
- **接口**: `PATCH /api/users/me/`
- **权限**: 需要登录
- **描述**: 更新当前用户的个人资料

#### 请求参数
```json
{
  "displayName": "New Name",
  "bio": "更新后的个人简介",
  "avatar": "https://example.com/new-avatar.jpg"
}
```

#### 参数说明
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| displayName | string | ❌ | 显示昵称 |
| bio | string | ❌ | 个人简介 |
| avatar | string | ❌ | 头像URL |

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "id": 1,
    "username": "testuser",
    "displayName": "New Name",
    "avatar": "https://example.com/new-avatar.jpg",
    "bio": "更新后的个人简介",
    "role": "USER",
    "followers": 12,
    "following": 5
  }
}
```

### 2.4 关注用户
- **接口**: `POST /api/users/{user_id}/follow/`
- **权限**: 需要登录
- **描述**: 关注指定用户

#### 成功响应 (200)
```json
{
  "code": 0,
  "message": "关注成功"
}
```

### 2.5 取消关注
- **接口**: `DELETE /api/users/{user_id}/follow/`
- **权限**: 需要登录
- **描述**: 取消关注指定用户

#### 成功响应 (200)
```json
{
  "code": 0,
  "message": "取消关注成功"
}
```

### 2.6 用户粉丝列表
- **接口**: `GET /api/users/{user_id}/followers/`
- **权限**: 游客可访问
- **描述**: 获取用户的粉丝列表

#### Query参数
- `page`: 页码（默认1）
- `pageSize`: 每页数量（默认20）

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "items": [
      {
        "follower": {
          "id": 2,
          "username": "follower1",
          "displayName": "关注者1",
          "avatar": "https://example.com/avatar1.jpg"
        },
        "followee": {
          "id": 1,
          "username": "testuser",
          "displayName": "Test User"
        },
        "created_at": "2026-01-29T10:00:00Z"
      }
    ],
    "page": 1,
    "pageSize": 20,
    "total": 1
  }
}
```

### 2.7 用户关注列表
- **接口**: `GET /api/users/{user_id}/following/`
- **权限**: 游客可访问
- **描述**: 获取用户关注的人列表

响应格式同粉丝列表。

### 2.8 投资偏好设置
- **接口**: `GET/PUT /api/users/me/invest-profile/`
- **权限**: 需要登录
- **描述**: 获取或设置用户投资偏好

#### PUT请求参数
```json
{
  "riskLevel": 2,
  "horizon": 3,
  "focusMarket": ["A", "US"],
  "preferredAssets": ["stock", "etf"]
}
```

#### 参数说明
| 字段 | 类型 | 说明 |
|------|------|------|
| riskLevel | int | 风险等级：1-低，2-中，3-高 |
| horizon | int | 投资期限：1-短期，2-中期，3-长期 |
| focusMarket | array | 关注市场：["A", "US", "HK"] |
| preferredAssets | array | 偏好资产：["stock", "etf", "fund"] |

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "riskLevel": 2,
    "horizon": 3,
    "focusMarket": ["A", "US"],
    "preferredAssets": ["stock", "etf"]
  }
}
```

### 2.9 用户收藏列表
- **接口**: `GET /api/users/me/favorites/`
- **权限**: 需要登录
- **描述**: 获取用户收藏的帖子列表

#### Query参数
- `page`: 页码
- `pageSize`: 每页数量

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "items": [
      {
        "id": 1,
        "authorId": 2,
        "authorName": "作者名称",
        "title": "帖子标题",
        "content": "帖子内容摘要",
        "tags": ["ETF", "策略"],
        "likes": 10,
        "comments": 5,
        "createdAt": "2026-01-29T10:00:00Z"
      }
    ],
    "total": 1
  }
}
```

### 2.10 用户举报列表
- **接口**: `GET /api/users/me/reports/`
- **权限**: 需要登录
- **描述**: 获取用户提交的举报列表

---

## 3. 内容管理 (Posts)

### 3.1 获取帖子列表
- **接口**: `GET /api/posts/`
- **权限**: 游客可访问（权限过滤）
- **描述**: 获取帖子列表，支持多种筛选条件

#### Query参数
| 参数 | 类型 | 说明 |
|------|------|------|
| status | string | 状态过滤，可多选：PUBLISHED,DRAFT |
| authorId | int | 作者ID筛选 |
| tag | string | 标签筛选 |
| q | string | 关键词搜索（标题/正文） |
| sort | string | 排序：new(最新) 或 hot(热门) |
| page | int | 页码，默认1 |
| pageSize | int | 每页数量，默认20 |

#### 权限过滤规则
- **游客**: 只能看到PUBLISHED状态的帖子
- **普通用户**: PUBLISHED全站 + 自己的所有状态帖子
- **管理员**: 可看到所有状态的帖子

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "items": [
      {
        "id": 10,
        "authorId": 1,
        "authorName": "作者昵称",
        "authorAvatar": "https://example.com/avatar.jpg",
        "title": "投资策略分享",
        "content": "这是帖子内容摘要...",
        "status": "PUBLISHED",
        "tags": ["ETF", "策略"],
        "likes": 12,
        "comments": 5,
        "createdAt": "2026-01-29T10:00:00Z",
        "assets": [
          {
            "id": 1,
            "code": "QQQ",
            "name": "Invesco QQQ",
            "type": "ETF"
          }
        ],
        "isLiked": false,
        "isFavorited": true
      }
    ],
    "page": 1,
    "pageSize": 20,
    "total": 100
  }
}
```

### 3.2 创建帖子
- **接口**: `POST /api/posts/`
- **权限**: 需要登录
- **描述**: 创建新帖子（草稿或提交审核）

#### 请求参数
```json
{
  "title": "我的投资心得",
  "content": "详细的帖子内容...",
  "tags": ["ETF", "策略", "心得"],
  "status": "DRAFT",
  "assetIds": [1, 2]
}
```

#### 参数说明
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | string | ✅ | 帖子标题 |
| content | string | ✅ | 帖子正文内容 |
| tags | array | ❌ | 标签数组 |
| status | string | ❌ | 状态：DRAFT(草稿) 或 PENDING_REVIEW(待审核) |
| assetIds | array | ❌ | 关联资产ID数组 |

#### 成功响应 (201)
```json
{
  "code": 0,
  "data": {
    "id": 11,
    "authorId": 1,
    "title": "我的投资心得",
    "content": "详细的帖子内容...",
    "status": "DRAFT",
    "tags": ["ETF", "策略", "心得"],
    "likes": 0,
    "comments": 0,
    "createdAt": "2026-01-29T10:00:00Z"
  }
}
```

### 3.3 获取帖子详情
- **接口**: `GET /api/posts/{post_id}/`
- **权限**: 游客可访问（状态受控）
- **描述**: 获取指定帖子的详细信息

#### 访问权限
- **游客/其他用户**: 只能访问PUBLISHED状态的帖子
- **作者**: 可访问自己所有状态的帖子
- **管理员**: 可访问所有状态的帖子

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "id": 10,
    "authorId": 1,
    "authorName": "作者昵称",
    "authorAvatar": "https://example.com/avatar.jpg",
    "title": "投资策略分享",
    "content": "完整的帖子内容...",
    "status": "PUBLISHED",
    "tags": ["ETF", "策略"],
    "likes": 12,
    "comments": 5,
    "createdAt": "2026-01-29T10:00:00Z",
    "assets": [
      {
        "id": 1,
        "code": "QQQ",
        "name": "Invesco QQQ",
        "type": "ETF"
      }
    ],
    "isLiked": false,
    "isFavorited": true,
    "comments": [
      {
        "id": 1,
        "authorId": 2,
        "authorName": "评论者",
        "body": "很好的分享！",
        "created_at": "2026-01-29T11:00:00Z",
        "replies": []
      }
    ]
  }
}
```

### 3.4 更新帖子
- **接口**: `PATCH /api/posts/{post_id}/`
- **权限**: 作者或管理员
- **描述**: 更新帖子内容或状态

#### 请求参数
```json
{
  "title": "更新后的标题",
  "content": "更新后的内容",
  "tags": ["新标签"],
  "status": "PENDING_REVIEW",
  "assetIds": [1, 3]
}
```

#### 权限说明
- **作者**: 建议只在DRAFT/REJECTED状态下修改内容
- **管理员**: 可修改任何状态的帖子

### 3.5 删除帖子
- **接口**: `DELETE /api/posts/{post_id}/`
- **权限**: 作者或管理员
- **描述**: 删除帖子（软删除，状态改为TAKEN_DOWN）

#### 成功响应 (200)
```json
{
  "code": 0,
  "message": "删除成功"
}
```

### 3.6 收藏帖子
- **接口**: `POST /api/posts/{post_id}/favorite/`
- **权限**: 需要登录
- **描述**: 收藏指定帖子

#### 成功响应 (200)
```json
{
  "code": 0,
  "message": "收藏成功"
}
```

### 3.7 取消收藏
- **接口**: `DELETE /api/posts/{post_id}/favorite/`
- **权限**: 需要登录
- **描述**: 取消收藏指定帖子

#### 成功响应 (200)
```json
{
  "code": 0,
  "message": "取消收藏成功"
}
```

---

## 4. 评论功能 (Comments)

### 4.1 获取评论列表
- **接口**: `GET /api/posts/{post_id}/comments/`
- **权限**: 游客可访问
- **描述**: 获取指定帖子的评论列表（支持楼中楼）

#### Query参数
- `page`: 页码
- `pageSize`: 每页数量

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": [
    {
      "id": 1,
      "authorId": 2,
      "authorName": "评论者",
      "authorAvatar": "https://example.com/avatar.jpg",
      "parentId": null,
      "replyToUserId": null,
      "body": "很好的分享！",
      "likeCount": 3,
      "createdAt": "2026-01-29T11:00:00Z",
      "isLiked": false,
      "replies": [
        {
          "id": 2,
          "authorId": 1,
          "authorName": "作者",
          "parentId": 1,
          "replyToUserId": 2,
          "replyToUsername": "评论者",
          "body": "谢谢支持！",
          "likeCount": 1,
          "createdAt": "2026-01-29T11:30:00Z",
          "isLiked": false,
          "replies": []
        }
      ]
    }
  ]
}
```

### 4.2 发表评论
- **接口**: `POST /api/posts/{post_id}/comments/`
- **权限**: 需要登录
- **描述**: 在指定帖子下发表评论或回复

#### 请求参数
```json
{
  "text": "我的评论内容",
  "parentId": null,
  "replyToUserId": null
}
```

#### 参数说明
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| text | string | ✅ | 评论内容 |
| parentId | int | ❌ | 父评论ID（回复时使用） |
| replyToUserId | int | ❌ | 被回复用户ID |

#### 成功响应 (201)
```json
{
  "code": 0,
  "data": {
    "id": 3,
    "authorId": 1,
    "authorName": "当前用户",
    "body": "我的评论内容",
    "parentId": null,
    "likeCount": 0,
    "createdAt": "2026-01-29T12:00:00Z",
    "isLiked": false,
    "replies": []
  }
}
```

### 4.3 删除评论
- **接口**: `DELETE /api/comments/{comment_id}/`
- **权限**: 评论作者或管理员
- **描述**: 删除指定评论

#### 成功响应 (200)
```json
{
  "code": 0,
  "message": "删除成功"
}
```

---

## 5. 点赞功能 (Likes)

### 5.1 点赞
- **接口**: `POST /api/likes/`
- **权限**: 需要登录
- **描述**: 对帖子、评论或组合进行点赞

#### 请求参数
```json
{
  "targetType": "POST",
  "targetId": 10
}
```

#### 参数说明
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| targetType | string | ✅ | 目标类型：POST/COMMENT/PORTFOLIO |
| targetId | int | ✅ | 目标ID |

#### 成功响应 (200)
```json
{
  "code": 0,
  "message": "点赞成功"
}
```

#### 错误响应
```json
{
  "code": 4090,
  "message": "已点赞过"
}
```

### 5.2 取消点赞
- **接口**: `DELETE /api/likes/`
- **权限**: 需要登录
- **描述**: 取消对帖子、评论或组合的点赞

#### 请求参数
```json
{
  "targetType": "POST",
  "targetId": 10
}
```

#### 成功响应 (200)
```json
{
  "code": 0,
  "message": "取消点赞成功"
}
```

---

## 6. 资产管理 (Assets)

### 6.1 搜索资产
- **接口**: `GET /api/assets/`
- **权限**: 游客可访问
- **描述**: 搜索股票、基金、ETF等资产

#### Query参数
| 参数 | 类型 | 说明 |
|------|------|------|
| type | string | 资产类型：STOCK/FUND/ETF/BOND |
| q | string | 搜索关键词（代码或名称） |
| page | int | 页码 |
| pageSize | int | 每页数量 |

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "items": [
      {
        "id": 1,
        "code": "000001",
        "name": "平安银行",
        "asset_type": "STOCK",
        "market": "A股"
      }
    ],
    "page": 1,
    "pageSize": 20,
    "total": 1
  }
}
```

### 6.2 资产详情
- **接口**: `GET /api/assets/{asset_id}/`
- **权限**: 游客可访问
- **描述**: 获取指定资产的详细信息

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "id": 1,
    "code": "000001",
    "name": "平安银行",
    "asset_type": "STOCK",
    "market": "A股"
  }
}
```

### 6.3 资产讨论区
- **接口**: `GET /api/assets/{asset_id}/posts/`
- **权限**: 游客可访问
- **描述**: 获取与指定资产相关的帖子列表

#### Query参数
- `sort`: 排序方式 new|hot
- `page`: 页码
- `pageSize`: 每页数量

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "items": [
      {
        "id": 10,
        "title": "平安银行投资分析",
        "authorName": "投资专家",
        "likes": 15,
        "comments": 8,
        "createdAt": "2026-01-29T10:00:00Z"
      }
    ],
    "page": 1,
    "pageSize": 20,
    "total": 5
  }
}
```

---

## 7. 投资组合 (Portfolios)

### 7.1 获取组合列表
- **接口**: `GET /api/portfolios/`
- **权限**: 游客可访问
- **描述**: 获取投资组合列表

#### Query参数
| 参数 | 类型 | 说明 |
|------|------|------|
| userId | int | 筛选指定用户的组合 |
| isPublic | boolean | 是否只显示公开组合 |
| sortBy | string | 排序：returnsYTD(收益率) 或 new(最新) |
| page | int | 页码 |
| pageSize | int | 每页数量 |

#### 权限说明
- **游客**: 只能看到公开组合
- **登录用户**: 公开组合 + 自己的私有组合

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "items": [
      {
        "id": 1,
        "userId": 1,
        "userName": "投资达人",
        "title": "科技ETF组合",
        "description": "专注科技股的长期投资组合",
        "riskLevel": "Medium",
        "returnsYTD": 0.12,
        "isPublic": true,
        "likes": 5,
        "assets": [
          {
            "symbol": "QQQ",
            "name": "Invesco QQQ",
            "allocation": 40
          },
          {
            "symbol": "SPY", 
            "name": "SPDR S&P 500",
            "allocation": 60
          }
        ],
        "isLiked": false,
        "createdAt": "2026-01-29T12:00:00Z"
      }
    ],
    "page": 1,
    "pageSize": 20,
    "total": 10
  }
}
```

### 7.2 获取热门组合
- **接口**: `GET /api/portfolios/top/`
- **权限**: 游客可访问
- **描述**: 获取收益率最高的热门公开组合

#### Query参数
- `limit`: 返回数量限制，默认5

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "items": [
      {
        "id": 1,
        "userName": "投资达人",
        "title": "高收益组合",
        "returnsYTD": 0.25,
        "riskLevel": "High",
        "likes": 20,
        "assets": [
          {
            "symbol": "TSLA",
            "name": "Tesla Inc",
            "allocation": 30
          }
        ]
      }
    ]
  }
}
```

### 7.3 创建组合
- **接口**: `POST /api/portfolios/`
- **权限**: 需要登录
- **描述**: 创建新的投资组合

#### 请求参数
```json
{
  "title": "我的均衡组合",
  "description": "追求稳健收益的均衡配置",
  "riskLevel": "Medium",
  "isPublic": true,
  "assets": [
    {
      "symbol": "SPY",
      "name": "SPDR S&P 500",
      "allocation": 60
    },
    {
      "symbol": "BND",
      "name": "Vanguard Total Bond",
      "allocation": 40
    }
  ]
}
```

#### 参数说明
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | string | ✅ | 组合名称 |
| description | string | ❌ | 组合描述 |
| riskLevel | string | ✅ | 风险等级：Low/Medium/High |
| isPublic | boolean | ❌ | 是否公开，默认true |
| assets | array | ✅ | 资产配置数组 |

#### 资产配置参数
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| symbol | string | ✅ | 资产代码 |
| name | string | ✅ | 资产名称 |
| allocation | number | ✅ | 配置比例(0-100) |

#### 校验规则
- allocation 必须在 0-100 之间
- 所有资产的 allocation 总和不能超过 100

#### 成功响应 (201)
```json
{
  "code": 0,
  "data": {
    "id": 2,
    "userId": 1,
    "title": "我的均衡组合",
    "description": "追求稳健收益的均衡配置",
    "riskLevel": "Medium",
    "isPublic": true,
    "likes": 0,
    "assets": [
      {
        "symbol": "SPY",
        "name": "SPDR S&P 500",
        "allocation": 60
      },
      {
        "symbol": "BND",
        "name": "Vanguard Total Bond",
        "allocation": 40
      }
    ],
    "createdAt": "2026-01-29T13:00:00Z"
  }
}
```

### 7.4 更新组合
- **接口**: `PATCH /api/portfolios/{portfolio_id}/`
- **权限**: 组合所有者或管理员
- **描述**: 更新投资组合信息

请求参数格式同创建组合。

### 7.5 删除组合
- **接口**: `DELETE /api/portfolios/{portfolio_id}/`
- **权限**: 组合所有者或管理员
- **描述**: 删除投资组合

#### 成功响应 (200)
```json
{
  "code": 0,
  "message": "删除成功"
}
```

### 7.6 点赞组合
使用统一点赞接口：
```json
POST /api/likes/
{
  "targetType": "PORTFOLIO",
  "targetId": 1
}
```

---

## 8. 举报功能 (Reports)

### 8.1 发起举报
- **接口**: `POST /api/reports/`
- **权限**: 需要登录
- **描述**: 举报帖子、评论或用户

#### 请求参数
```json
{
  "targetType": "POST",
  "targetId": 10,
  "reason": "疑似广告/不实信息"
}
```

#### 参数说明
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| targetType | string | ✅ | 举报目标：POST/COMMENT/USER |
| targetId | int | ✅ | 目标ID |
| reason | string | ✅ | 举报原因 |

#### 成功响应 (201)
```json
{
  "code": 0,
  "message": "举报提交成功",
  "data": {
    "id": 1
  }
}
```

### 8.2 我的举报列表
- **接口**: `GET /api/users/me/reports/`
- **权限**: 需要登录
- **描述**: 查看自己提交的举报记录

#### Query参数
- `page`: 页码
- `pageSize`: 每页数量

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "items": [
      {
        "id": 1,
        "reporterName": "当前用户",
        "targetType": "POST",
        "targetId": 10,
        "reason": "疑似广告",
        "status": "PENDING",
        "createdAt": "2026-01-29T10:00:00Z"
      }
    ],
    "total": 1
  }
}
```

---

## 9. 通知系统 (Notifications)

### 9.1 获取通知列表
- **接口**: `GET /api/notifications/`
- **权限**: 需要登录
- **描述**: 获取用户的通知列表

#### Query参数
| 参数 | 类型 | 说明 |
|------|------|------|
| unreadOnly | boolean | 是否只显示未读通知 |
| page | int | 页码 |
| pageSize | int | 每页数量 |

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "items": [
      {
        "id": 1,
        "notification_type": "LIKE",
        "title": "有人点赞了你的帖子",
        "content": "用户"投资新手"点赞了你的帖子"投资心得分享"",
        "related_object_type": "POST",
        "related_object_id": 10,
        "is_read": false,
        "created_at": "2026-01-29T10:00:00Z",
        "read_at": null
      },
      {
        "id": 2,
        "notification_type": "COMMENT",
        "title": "有人评论了你的帖子",
        "content": "用户"资深投资者"在你的帖子下发表了评论",
        "is_read": true,
        "created_at": "2026-01-29T09:00:00Z",
        "read_at": "2026-01-29T09:30:00Z"
      }
    ],
    "page": 1,
    "pageSize": 20,
    "total": 2
  }
}
```

### 9.2 标记通知已读
- **接口**: `POST /api/notifications/{notification_id}/read/`
- **权限**: 需要登录
- **描述**: 标记指定通知为已读

#### 成功响应 (200)
```json
{
  "code": 0,
  "message": "标记成功"
}
```

### 9.3 一键已读
- **接口**: `POST /api/notifications/read-all/`
- **权限**: 需要登录
- **描述**: 标记所有通知为已读

#### 成功响应 (200)
```json
{
  "code": 0,
  "message": "标记了5条通知为已读"
}
```

---

## 10. Dashboard (数据概览)

### 10.1 概览数据
- **接口**: `GET /api/dashboard/overview/`
- **权限**: 游客可访问
- **描述**: 获取社区首页展示的聚合数据

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "marketSeries": [
      { "name": "Mon", "value": 4200 },
      { "name": "Tue", "value": 4150 },
      { "name": "Wed", "value": 4300 },
      { "name": "Thu", "value": 4250 },
      { "name": "Fri", "value": 4400 }
    ],
    "trendingPosts": [
      {
        "id": 1,
        "title": "本周市场热点分析",
        "authorName": "投资分析师",
        "likes": 25,
        "comments": 12,
        "createdAt": "2026-01-29T10:00:00Z"
      }
    ],
    "topPortfolios": [
      {
        "id": 1,
        "title": "科技成长组合",
        "userName": "成长投资者",
        "returnsYTD": 0.18,
        "likes": 15
      }
    ],
    "communityStats": {
      "activeInvestorsCount": 1234,
      "strategiesSharedCount": 321
    }
  }
}
```

---

## 11. 全局搜索

### 11.1 搜索
- **接口**: `GET /api/search/`
- **权限**: 游客可访问
- **描述**: 全站搜索帖子、资产、投资组合

#### Query参数
| 参数 | 类型 | 说明 |
|------|------|------|
| q | string | 搜索关键词 |
| type | string | 搜索类型：all/post/asset/portfolio |
| page | int | 页码 |
| pageSize | int | 每页数量 |

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "posts": {
      "items": [
        {
          "id": 1,
          "title": "ETF投资指南",
          "authorName": "投资专家",
          "likes": 20,
          "comments": 8
        }
      ],
      "total": 5
    },
    "assets": {
      "items": [
        {
          "id": 1,
          "code": "QQQ",
          "name": "Invesco QQQ",
          "type": "ETF"
        }
      ],
      "total": 2
    },
    "portfolios": {
      "items": [
        {
          "id": 1,
          "title": "ETF组合策略",
          "userName": "组合达人",
          "returnsYTD": 0.15
        }
      ],
      "total": 3
    }
  }
}
```

---

## 12. 管理员功能 (Admin)

> **权限要求**: 仅 MODERATOR 或 ADMIN 角色可访问

### 12.1 获取待审核帖子
- **接口**: `GET /api/admin/posts/`
- **权限**: 管理员
- **描述**: 获取需要审核的帖子列表

#### Query参数
| 参数 | 类型 | 说明 |
|------|------|------|
| status | string | 状态筛选：PENDING_REVIEW |
| page | int | 页码 |
| pageSize | int | 每页数量 |

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "items": [
      {
        "id": 5,
        "authorName": "新用户",
        "title": "我的投资想法",
        "content": "帖子内容...",
        "status": "PENDING_REVIEW",
        "createdAt": "2026-01-29T10:00:00Z",
        "tags": ["投资", "想法"]
      }
    ],
    "page": 1,
    "pageSize": 20,
    "total": 3
  }
}
```

### 12.2 审核帖子
- **接口**: `PATCH /api/admin/posts/{post_id}/status/`
- **权限**: 管理员
- **描述**: 修改帖子状态（通过/驳回/下架）

#### 审核通过
```json
{
  "status": "PUBLISHED"
}
```

#### 审核驳回
```json
{
  "status": "REJECTED",
  "rejectReason": "内容不符合社区规范"
}
```

#### 违规下架
```json
{
  "status": "TAKEN_DOWN",
  "rejectReason": "违反社区规定，予以下架"
}
```

#### 成功响应 (200)
```json
{
  "code": 0,
  "message": "状态更新成功"
}
```

### 12.3 待处理举报
- **接口**: `GET /api/admin/reports/`
- **权限**: 管理员
- **描述**: 获取待处理的举报列表

#### Query参数
- `status`: PENDING 或 RESOLVED
- `page`: 页码
- `pageSize`: 每页数量

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "items": [
      {
        "id": 1,
        "reporterName": "举报用户",
        "targetType": "POST",
        "targetId": 10,
        "reason": "疑似广告",
        "status": "PENDING",
        "createdAt": "2026-01-29T10:00:00Z"
      }
    ],
    "page": 1,
    "pageSize": 20,
    "total": 2
  }
}
```

### 12.4 处理举报
- **接口**: `PATCH /api/admin/reports/{report_id}/`
- **权限**: 管理员
- **描述**: 处理举报（标记为已解决）

#### 请求参数
```json
{
  "status": "RESOLVED",
  "result": "VALID",
  "handleResult": "已对相关帖子进行下架处理"
}
```

#### 成功响应 (200)
```json
{
  "code": 0,
  "message": "处理成功"
}
```

### 12.5 管理统计
- **接口**: `GET /api/admin/stats/`
- **权限**: 管理员
- **描述**: 获取管理后台统计数据

#### 成功响应 (200)
```json
{
  "code": 0,
  "data": {
    "pendingPostsCount": 12,
    "openReportsCount": 3,
    "newUsers24h": 42
  }
}
```

### 12.6 风控告警
- **接口**: `GET /api/admin/alerts/`
- **权限**: 管理员
- **描述**: 获取风控告警信息

#### Query参数
- `status`: OPEN 或 RESOLVED

---

# 🧪 完整测试指南

## Apifox配置

### 环境设置
创建开发环境，配置以下变量：
```
base_url: http://127.0.0.1:8000
access_token: (通过登录获取)
refresh_token: (通过登录获取)
user_id: (当前用户ID)
```

### 全局请求头
```json
{
  "Content-Type": "application/json",
  "Accept": "application/json"
}
```

### 认证请求头模板
对于需要登录的接口：
```
Authorization: Bearer {{access_token}}
```

## 测试步骤

### Phase 1: 基础认证测试
1. **用户注册** → 保存返回的tokens
2. **用户登录** → 验证token获取
3. **获取用户信息** → 验证token使用
4. **Token刷新** → 验证token刷新机制

### Phase 2: 内容管理测试
1. **创建帖子** (草稿/待审核状态)
2. **获取帖子列表** (验证权限过滤)
3. **获取帖子详情** (验证访问控制)
4. **更新帖子** (测试状态变更)
5. **点赞功能** (点赞/取消点赞)
6. **收藏功能** (收藏/取消收藏)
7. **评论功能** (发表/回复/删除)

### Phase 3: 高级功能测试
1. **投资组合** (创建/更新/删除/点赞)
2. **用户关注** (关注/取消关注/关系列表)
3. **举报功能** (发起举报/查看举报)
4. **资产搜索** (搜索/详情/相关帖子)
5. **通知系统** (通知列表/标记已读)

### Phase 4: 管理功能测试
1. **管理员登录** (使用ADMIN角色账号)
2. **内容审核** (审核帖子状态)
3. **举报处理** (处理用户举报)
4. **统计数据** (管理后台数据)

### Phase 5: 搜索和聚合测试
1. **全局搜索** (多类型搜索)
2. **Dashboard数据** (首页聚合数据)
3. **热门内容** (热门帖子/组合)

## 测试数据建议

### 用户数据
```json
// 普通用户
{
  "username": "test_user_001",
  "email": "user001@test.com",
  "password": "test123456",
  "password_confirm": "test123456",
  "phone": "13800138001"
}

// 管理员用户（需在Django Admin中手动设置role为ADMIN）
{
  "username": "admin_001",
  "email": "admin@test.com", 
  "password": "admin123456",
  "password_confirm": "admin123456"
}
```

### 帖子数据
```json
{
  "title": "ETF投资策略分享",
  "content": "详细介绍ETF投资的优势和注意事项...",
  "tags": ["ETF", "投资策略", "理财"],
  "status": "DRAFT"
}
```

### 投资组合数据
```json
{
  "title": "稳健型ETF组合",
  "description": "适合风险厌恶型投资者的均衡配置",
  "riskLevel": "Low",
  "isPublic": true,
  "assets": [
    {
      "symbol": "SPY",
      "name": "SPDR S&P 500 ETF",
      "allocation": 60
    },
    {
      "symbol": "BND", 
      "name": "Vanguard Total Bond Market ETF",
      "allocation": 40
    }
  ]
}
```

## 权限测试矩阵

| 功能 | 游客 | 普通用户 | 管理员 |
|------|------|----------|--------|
| 查看公开帖子 | ✅ | ✅ | ✅ |
| 创建帖子 | ❌ | ✅ | ✅ |
| 审核帖子 | ❌ | ❌ | ✅ |
| 处理举报 | ❌ | ❌ | ✅ |
| 查看统计 | ❌ | ❌ | ✅ |

## 常见测试场景

### 边界值测试
- 超长标题/内容
- 空值/null值处理
- 特殊字符处理
- 大数值处理

### 异常流程测试
- 无效Token访问
- 权限不足操作
- 重复操作（重复点赞/关注）
- 资源不存在访问

### 并发测试
- 同时点赞同一帖子
- 同时关注同一用户
- 并发创建内容

---

# 🎯 总结

本文档提供了投研社区后端API的完整规范，包含：

- ✅ **45个API接口** 的详细说明
- ✅ **完整的请求响应示例**
- ✅ **权限控制规则**
- ✅ **错误处理机制**
- ✅ **完整的测试指南**

所有接口均已实现并测试通过，可直接用于前端开发和接口调试。

## 快速开始
1. 启动Django服务器：`python manage.py runserver`
2. 导入本文档到Apifox进行接口测试
3. 从用户注册开始，按照测试步骤逐步验证功能
4. 根据实际需求调用相应的API接口

**🚀 现在就开始您的API测试之旅吧！**