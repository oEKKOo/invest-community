---
alwaysApply: true
---

# 投研社区 + Finnhub 实时行情 数据库完整文档（合并版）

> 本文档将原有投研社区核心库（`mysql.mdc`）与 Finnhub 行情扩展（`数据库.mdc`）合并为统一开发参考文档。
> 技术栈：Django + Django REST Framework + MySQL 8.x + InnoDB + utf8mb4

---

## 一、全局约定

### 1.1 系统背景

本系统是一个 **投研社区 + 投顾组合一体化 Web 应用**，支持：

* 用户发帖交流（Community）
* 投资组合创建与展示（Portfolios）
* 管理员内容审核与风控（Admin Panel）
* 社区趋势与数据概览（Dashboard）
* **实时行情展示（Finnhub 数据接入）** ← 新增

### 1.2 设计目标

1. **支撑前端 UI 真实需求**
2. **满足毕业设计论文规范**
3. **核心功能优先，预留扩展空间**
4. **状态清晰、职责单一、可维护**

### 1.3 字符集与引擎

* 统一：`InnoDB + utf8mb4`
* 时间字段统一：`created_at / updated_at`（可选 `deleted_at` 软删）
* 主键统一 `BIGINT` 自增

### 1.4 关键业务原则（"真实 code 贯穿全系统"）

| 层级 | 使用字段 | 说明 |
| --- | --- | --- |
| 用户输入/展示层 | `asset.code + asset.market` | 如 600519 + SH |
| 第三方 Finnhub 调用层 | `asset.finnhub_symbol` | 唯一映射，调 API 专用 |
| 内容/评论/组合等业务层 | `asset_id` | 避免 code 变更引发全库污染 |

---

## 二、表清单总览

### 2.1 用户与权限域

| # | 表名 | 说明 |
| --- | --- | --- |
| 1 | `user` | 用户账户、角色、状态 |
| 2 | `user_invest_profile` | 投资偏好画像（1:1） |
| 3 | `user_follow` | 关注/粉丝关系 |

### 2.2 内容与互动域

| # | 表名 | 说明 |
| --- | --- | --- |
| 4 | `content` | 帖子/长文（含审核状态机） |
| 5 | `content_asset` | 内容与标的关联（讨论区聚合） |
| 6 | `comment` | 评论/楼中楼 |
| 7 | `likes` | 点赞（帖子/评论/组合） |
| 8 | `favorite` | 收藏 |
| 9 | `report` | 举报/内容治理 |

### 2.3 资产与行情域

| # | 表名 | 说明 |
| --- | --- | --- |
| 10 | `asset` | 标的资产库（已增强 Finnhub 映射字段） |
| 11 | `asset_quote_snapshot` | 行情快照（MVP 核心，对接 Finnhub `/quote`） |
| 12 | `asset_kline` | K线数据（对接 Finnhub `/stock/candle`） |

### 2.4 组合与扩展域

| # | 表名 | 说明 |
| --- | --- | --- |
| 13 | `portfolio` | 投资组合 |
| 14 | `portfolio_asset` | 组合内资产配置 |
| 15 | `notification` | 通知系统（扩展） |
| 16 | `alert` | 风控告警（扩展） |

> 🔹 **核心必需表：1-14**
> 🔹 **扩展增强表：notification / alert**

---

## 三、核心实体关系（ER 文字说明）

* 一个 **用户（user）**
  * 可以发布多条 **内容（content）**
  * 可以创建多个 **投资组合（portfolio）**
  * 可以关注多个用户（user_follow）
* 一条 **内容（content）**
  * 可关联多个 **标的（asset）**（通过 content_asset）
  * 可被多次点赞 / 收藏 / 评论
* 一个 **组合（portfolio）**
  * 包含多条 **组合资产（portfolio_asset）**
  * 可被点赞
* 一个 **标的（asset）**
  * 拥有多条行情快照（asset_quote_snapshot）
  * 拥有多根 K 线（asset_kline）
* 举报（report）可指向：内容 / 评论 / 用户

---

## 四、详细表结构说明

---

### 4.1 `user`（用户表）

**用途**：账户信息、角色权限、用户状态。支撑权限控制（USER / ADMIN）。

**核心字段**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BIGINT PK | 用户主键 |
| `username` | VARCHAR(32) | 登录用户名，唯一 |
| `display_name` | VARCHAR(64) | 展示昵称 |
| `email` | VARCHAR(128) | 邮箱，唯一 |
| `phone` | VARCHAR(20) | 手机号，唯一 |
| `password_hash` | VARCHAR(255) | 密码哈希 |
| `avatar_url` | VARCHAR(255) | 头像 URL |
| `bio` | VARCHAR(300) | 个人简介 |
| `role` | TINYINT | 0 普通用户，1 管理员 |
| `status` | TINYINT | 0 正常，1 禁言，2 封禁 |
| `mute_until` | DATETIME | 禁言截止时间 |
| `last_login_at` | DATETIME | 最后登录时间 |
| `created_at` | DATETIME | 注册时间 |
| `updated_at` | DATETIME | 更新时间（ON UPDATE） |

**索引/约束**

* `username`、`email`、`phone` 唯一约束
* 索引：`status`、`role`

---

### 4.2 `user_invest_profile`（用户投资偏好，1:1）

**用途**：用于推荐 / 用户画像（论文加分点）。

**核心字段**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `user_id` | BIGINT PK | 外键 → user.id（1:1） |
| `risk_level` | TINYINT | 1 低，2 中，3 高 |
| `horizon` | TINYINT | 1 短，2 中，3 长 |
| `focus_market` | JSON | 如 ["A", "HK", "US"] |
| `preferred_assets` | JSON | 如 ["stock", "etf", "fund"] |
| `updated_at` | DATETIME | 更新时间 |

**约束**：`user_id` 唯一（一个用户一份画像）

---

### 4.3 `user_follow`（关注表）

**用途**：用户社交关系（关注 / 粉丝）。

**核心字段**

| 字段 | 说明 |
| --- | --- |
| `follower_id` | 关注者（FK → user.id） |
| `followee_id` | 被关注者（FK → user.id） |
| `created_at` | 关注时间 |

**约束**：`(follower_id, followee_id)` 复合主键防重
**索引**：`followee_id`（查某人粉丝列表）

---

### 4.4 `asset`（标的资产库）

**用途**：系统中"股票/基金/ETF"的权威表，**真实 code 的核心承载**。

**核心字段（基础）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BIGINT PK | 资产主键 |
| `asset_type` | TINYINT | 0 股票，1 基金，2 ETF |
| `code` | VARCHAR(20) | 用户输入/展示的自然码（如 600519、AAPL） |
| `name` | VARCHAR(64) | 资产名称 |
| `market` | VARCHAR(16) | 业务侧市场：SH/SZ/HK/US |
| `status` | TINYINT | 0 正常，1 停用 |
| `created_at` | DATETIME | 创建时间 |

**增强字段（对接 Finnhub）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `finnhub_symbol` | VARCHAR(32) | ⭐ 调用 Finnhub 的唯一标识，唯一约束（允许 NULL） |
| `exchange` | VARCHAR(32) | 交易所（用于 symbols 导入/筛选，如 US/HK） |
| `currency` | VARCHAR(8) | 币种：USD/CNY/HKD |
| `last_sync_at` | DATETIME | 基础信息同步时间 |

**基金可选增强**

| 字段 | 说明 |
| --- | --- |
| `isin` | ISIN 编码 |
| `fund_type` | 基金类型 |
| `meta_json` | 扩展 JSON（logo/行业/官网等） |

**关键约束与索引**

* 唯一：`(asset_type, code)`
* 唯一：`finnhub_symbol`（允许 NULL）
* 索引：`code`、`name`、`market`、`exchange`

---

### 4.5 `asset_quote_snapshot`（行情快照表）⭐ 新增

**用途**：落地 Finnhub `/quote` 结果，用于：
* 前端刷新时先读 DB/缓存，降压第三方 API
* 论文工程优化亮点：快照表 + 缓存 + 定时刷新

**核心字段（对齐 Finnhub Quote 响应）**

| 字段 | 类型 | Finnhub字段 | 说明 |
| --- | --- | --- | --- |
| `id` | BIGINT PK | - | 主键 |
| `asset_id` | BIGINT | - | FK → asset.id |
| `price` | DECIMAL(18,6) | `c` | 最新价 |
| `change_amount` | DECIMAL(18,6) | `d` | 涨跌额 |
| `change_pct` | DECIMAL(9,4) | `dp` | 涨跌幅（%） |
| `open` | DECIMAL(18,6) | `o` | 开盘价 |
| `high` | DECIMAL(18,6) | `h` | 最高价 |
| `low` | DECIMAL(18,6) | `l` | 最低价 |
| `prev_close` | DECIMAL(18,6) | `pc` | 昨收价 |
| `volume` | BIGINT | - | 成交量（可选） |
| `amount` | DECIMAL(20,2) | - | 成交额（可选） |
| `quote_time` | DATETIME | `t`（转换） | 行情时间 |
| `source` | VARCHAR(16) | - | 数据来源，默认 'finnhub' |
| `created_at` | DATETIME | - | 写入时间 |

**关键索引**

* `(asset_id, quote_time)`：查最新快照 `ORDER BY quote_time DESC LIMIT 1`
* `quote_time`：定期清理历史数据

**数据保留策略**

* MVP：保留 1~7 天快照
* 清理任务：按 `quote_time` 过期删除

---

### 4.6 `asset_kline`（K 线表）⭐ 新增

**用途**：落地 Finnhub `/stock/candle` 的 OHLCV，支持 ECharts K 线图。

**核心字段**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BIGINT PK | 主键 |
| `asset_id` | BIGINT | FK → asset.id |
| `resolution` | VARCHAR(8) | 周期：1/5/15/30/60/D/W/M |
| `k_time` | DATETIME | K 线时间点（由 `t` 转换） |
| `open` | DECIMAL(18,6) | 开盘价 |
| `high` | DECIMAL(18,6) | 最高价 |
| `low` | DECIMAL(18,6) | 最低价 |
| `close` | DECIMAL(18,6) | 收盘价 |
| `volume` | BIGINT | 成交量 |
| `created_at` | DATETIME | 写入时间 |

**关键约束**

* 唯一：`(asset_id, resolution, k_time)`（防止重复写入）

**关键索引**

* `(asset_id, resolution, k_time)`：查询最近 N 根 K 线

**MVP 策略**

* 先只做 `resolution = D`（日 K）
* 保留最近 200 根（足够画图 + 均线计算）

---

### 4.7 `content`（帖子/长文）

**用途**：统一承载社区内容，内置审核状态机。

**内容状态机**

| 数值 | 状态 | 说明 |
| --- | --- | --- |
| 0 | DRAFT | 草稿 |
| 1 | PENDING_REVIEW | 待审核 |
| 2 | PUBLISHED | 已发布 |
| 3 | REJECTED | 被驳回 |
| 4 | TAKEN_DOWN | 下架 |
| 5 | DELETED | 删除 |

**核心字段**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BIGINT PK | 内容 ID |
| `author_id` | BIGINT | FK → user.id |
| `content_type` | TINYINT | 0 帖子，1 长文 |
| `title` | VARCHAR(120) | 标题 |
| `body` | MEDIUMTEXT | 正文 |
| `summary` | VARCHAR(300) | 摘要 |
| `cover_url` | VARCHAR(255) | 封面图 |
| `visibility` | TINYINT | 0 公开，1 仅粉丝，2 私密 |
| `status` | TINYINT | 内容状态（见上表） |
| `reviewed_by` | BIGINT | 审核管理员 ID |
| `reviewed_at` | DATETIME | 审核时间 |
| `reject_reason` | VARCHAR(255) | 驳回原因 |
| `like_count` | INT | 点赞数（冗余） |
| `comment_count` | INT | 评论数（冗余） |
| `favorite_count` | INT | 收藏数（冗余） |
| `view_count` | INT | 浏览数 |
| `created_at` | DATETIME | 创建时间 |
| `updated_at` | DATETIME | 更新时间 |
| `published_at` | DATETIME | 发布时间 |

**索引**

* `(author_id, created_at)`
* `(status, published_at)`
* `(content_type, status, published_at)`

---

### 4.8 `content_asset`（内容-标的关联，讨论区聚合关键）

**用途**：把帖子与真实标的绑定，形成"某股票/基金讨论区"。

| 字段 | 说明 |
| --- | --- |
| `content_id` | FK → content.id |
| `asset_id` | FK → asset.id |
| `created_at` | 关联时间 |

**约束**：`(content_id, asset_id)` 复合主键防重复
**索引**：`asset_id`（查询某标的下所有帖子）

---

### 4.9 `comment`（评论/楼中楼）

**用途**：支持帖子一级评论 + 二级回复（楼中楼）。

**核心字段**

| 字段 | 说明 |
| --- | --- |
| `id` | 评论 ID |
| `content_id` | FK → content.id |
| `author_id` | FK → user.id |
| `parent_id` | 父评论 ID（NULL=一级评论） |
| `reply_to_user_id` | 被回复用户 ID |
| `body` | 评论内容 |
| `status` | 0 正常，1 屏蔽，2 删除 |
| `like_count` | 点赞数（冗余） |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

**索引**

* `(content_id, created_at)`
* `(parent_id, created_at)`
* `(author_id, created_at)`

---

### 4.10 `likes`（点赞表）

**用途**：统一点赞帖子 / 评论 / 组合。

**核心字段**

| 字段 | 说明 |
| --- | --- |
| `id` | 主键 |
| `user_id` | FK → user.id |
| `target_type` | 0 内容，1 评论 |
| `target_id` | 目标 ID |
| `created_at` | 时间 |

**约束**：`(user_id, target_type, target_id)` 唯一（防重复点赞）
**索引**：`(target_type, target_id)`

---

### 4.11 `favorite`（收藏表）

**用途**：收藏帖子/长文。

**核心字段**

| 字段 | 说明 |
| --- | --- |
| `id` | 主键 |
| `user_id` | FK → user.id |
| `content_id` | FK → content.id |
| `created_at` | 时间 |

**约束**：`(user_id, content_id)` 唯一

---

### 4.12 `report`（举报表）

**用途**：社区治理与风控闭环。

**核心字段**

| 字段 | 说明 |
| --- | --- |
| `id` | 主键 |
| `reporter_id` | FK → user.id |
| `target_type` | 0 内容，1 评论，2 用户 |
| `target_id` | 目标 ID |
| `reason_type` | 1 广告，2 辱骂，3 不实信息，4 违法，5 其他 |
| `reason_detail` | 详细说明 |
| `status` | 0 待处理，1 有效，2 无效 |
| `handler_id` | 处理管理员 |
| `handle_result` | 处理结果 |
| `created_at` | 举报时间 |
| `handled_at` | 处理时间 |

**索引**：`(status, created_at)`（后台队列）

---

### 4.13 `portfolio`（投资组合）

**用途**：投顾组合核心实体。

**核心字段**

| 字段 | 说明 |
| --- | --- |
| `id` | 组合 ID |
| `owner_id` | FK → user.id |
| `title` | 组合名称 |
| `description` | 描述 |
| `risk_level` | ENUM：Low/Medium/High |
| `returns_ytd` | 年初至今回报（DECIMAL） |
| `is_public` | 是否公开 |
| `status` | 0 正常，1 隐藏，2 删除 |
| `like_count` | 点赞数（冗余） |
| `created_at / updated_at` | 时间 |

**索引**

* `(owner_id, created_at)`
* `(is_public, returns_ytd)`

---

### 4.14 `portfolio_asset`（组合资产配置）

**用途**：组合中每一项资产配置。

**核心字段**

| 字段 | 说明 |
| --- | --- |
| `id` | 主键 |
| `portfolio_id` | FK → portfolio.id |
| `symbol` | 资产代码 |
| `name` | 资产名称 |
| `allocation` | 仓位百分比（0~100） |
| `created_at` | 时间 |

**约束**：`(portfolio_id, symbol)` 唯一

---

### 4.15 `notification`（通知系统，扩展）

**用途**：支撑右上角铃铛通知。

| 通知类型 | 触发场景 |
| --- | --- |
| `LIKE` | 有人点赞你的帖子/评论 |
| `COMMENT` | 有人评论你的帖子 |
| `FOLLOW` | 有人关注了你 |
| `REVIEW_RESULT` | 内容审核结果通知 |
| `SYSTEM` | 系统公告 |

**核心字段**

| 字段 | 说明 |
| --- | --- |
| `id` | 主键 |
| `user_id` | 接收者 FK → user.id |
| `type` | 通知类型（ENUM） |
| `payload` | JSON（postId/commentId/fromUserId 等） |
| `is_read` | 0 未读，1 已读 |
| `created_at` | 时间 |

**索引**：`(user_id, is_read, created_at)`

---

### 4.16 `alert`（风控告警，扩展）

**用途**：Admin Panel 风险提醒 / AI 审核异常标记。

| 字段 | 说明 |
| --- | --- |
| `id` | 主键 |
| `type` | ENUM：AI_RISK/SPAM/POLICY/SYSTEM |
| `message` | 告警信息 |
| `source_type` | POST/COMMENT/USER |
| `source_id` | 来源 ID |
| `status` | OPEN/CLOSED |
| `created_at` | 时间 |

**索引**：`(status, created_at)`

---

## 五、数据库与 API 模块映射

| 前端模块 | 主要表 |
| --- | --- |
| Dashboard | `content` / `portfolio` / `user` |
| Community（社区） | `content` / `comment` / `likes` / `asset` |
| 讨论区（标的） | `asset` / `content_asset` / `content` |
| 行情展示 | `asset` / `asset_quote_snapshot` / `asset_kline` |
| Portfolios | `portfolio` / `portfolio_asset` |
| Admin Panel | `content` / `report` / `alert` / `user` |
| Profile | `user` / `content` / `portfolio` / `favorite` |
| 通知中心 | `notification` |

---

## 六、典型业务查询场景

### 6.1 标的详情页（基础信息 + 最新行情）

```sql
-- 1. 读取标的基础信息
SELECT * FROM asset WHERE id = ?;

-- 2. 最新行情快照（优先读缓存，缓存 miss 再查库）
SELECT * FROM asset_quote_snapshot
WHERE asset_id = ?
ORDER BY quote_time DESC
LIMIT 1;

-- 若无快照：后端去 Finnhub 拉取并写入 asset_quote_snapshot
```

### 6.2 标的讨论区（内容聚合）

```sql
-- 按标的聚合该标的相关帖子
SELECT c.* FROM content c
JOIN content_asset ca ON ca.content_id = c.id
WHERE ca.asset_id = ? AND c.status = 2
ORDER BY c.published_at DESC;
```

### 6.3 发帖绑定标的流程

1. 前端 AssetSelect 搜索：`GET /api/assets/?q=关键词`（查 `asset` 表）
2. 用户选定后提交 `assetIds[]`
3. 后端写 `content` 记录，再批量写 `content_asset`

### 6.4 K 线图数据（日 K）

```sql
SELECT k_time, open, high, low, close, volume
FROM asset_kline
WHERE asset_id = ? AND resolution = 'D'
ORDER BY k_time DESC
LIMIT 200;
```

---

## 七、后续可扩展方向

1. **积分系统**：`user_points` / `points_log`
2. **全文搜索**：MySQL FULLTEXT 索引 或 Elasticsearch
3. **AI 风控命中记录表**：记录 AI 模型审核结果
4. **消息队列**：行情定时刷新（Celery Beat → 写 `asset_quote_snapshot`）
5. **WebSocket / SSE**：实时推送行情与评论

---
