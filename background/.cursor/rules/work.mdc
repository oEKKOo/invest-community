---
alwaysApply: true
---
# 股票/基金实时行情接入：后端接口扩展与改动清单（基于你现有接口文档）

> 依据你当前已完成的接口文档中 **Assets 模块仅包含：搜索 / 详情 / 讨论区(posts)**，以及 **Posts 创建已支持 assetIds** 的现状，下面给出“为实时行情 + 代码化分享”需要**新增**与**改动**的接口清单。

---

## 1. 需要新增的接口（Real-time Quote & Chart Data）

### 1.1 获取资产最新行情（单资产）

* **接口**：`GET /api/assets/{asset_id}/quote/`
* **权限**：游客可访问
* **目的**：资产详情页顶部“最新价/涨跌幅/更新时间”等；前端可轮询或做 SSE 推送前的基础数据源

**响应示例**

```json
{
  "code": 0,
  "data": {
    "assetId": 1,
    "code": "000001",
    "name": "平安银行",
    "quoteTime": "2026-02-20T20:00:01Z",
    "price": 12.34,
    "change": 0.15,
    "changePct": 0.0123,
    "open": 12.10,
    "high": 12.50,
    "low": 12.00,
    "prevClose": 12.19,
    "volume": 123456789,
    "amount": 987654321
  }
}
```

---

### 1.2 获取K线数据（用于 ECharts K线图）

* **接口**：`GET /api/assets/{asset_id}/kline/`
* **权限**：游客可访问
* **Query 参数**

  * `interval`: `1d|60m|15m|5m|1m`（你按数据源能力裁剪）
  * `limit`: 默认 200
  * `from` / `to`（可选，ISO 时间或日期）

**响应示例**

```json
{
  "code": 0,
  "data": {
    "assetId": 1,
    "interval": "1d",
    "items": [
      {
        "time": "2026-02-19",
        "open": 12.10,
        "high": 12.50,
        "low": 11.90,
        "close": 12.30,
        "volume": 12345678
      }
    ]
  }
}
```

---

### 1.3 获取分时数据（用于分钟线/当日走势）

* **接口**：`GET /api/assets/{asset_id}/intraday/`
* **权限**：游客可访问
* **Query 参数**

  * `date`: `YYYY-MM-DD`（默认当天）
  * `interval`: `1m|5m`（可选）

**响应示例**

```json
{
  "code": 0,
  "data": {
    "assetId": 1,
    "date": "2026-02-20",
    "items": [
      { "time": "09:31", "price": 12.21, "avgPrice": 12.18, "volume": 1200 }
    ]
  }
}
```

---

### 1.4 批量获取资产最新行情（列表页/组合页必备，强烈建议）

* **接口**：`POST /api/assets/quotes/`
* **权限**：游客可访问（也可登录）
* **目的**：资产列表页、Portfolio 持仓列表一次请求拿到多个 code 的最新价，避免前端 N 次调用

**请求**

```json
{ "assetIds": [1, 2, 3] }
```

**响应**

```json
{
  "code": 0,
  "data": {
    "items": [
      { "assetId": 1, "price": 12.34, "changePct": 0.0123, "quoteTime": "..." }
    ]
  }
}
```

---

### 1.5 （可选增强）行情推送：SSE（更适合“实时网站”展示）

* **接口**：`GET /api/assets/{asset_id}/quote/stream/`
* **权限**：游客可访问
* **返回**：`text/event-stream`
* **说明**：前端 EventSource 接入；服务端可按 1s/3s 推送一次最新 quote（数据来自缓存/快照）

> 如果你短期不做推送，也可以先不上这个接口；轮询 `quote/` 就能满足演示。

---

## 2. 需要改动的既有接口（为了“真实 code 贯穿分享与讨论”）

### 2.1 改动：资产搜索接口返回行情字段（可选但非常推荐）

你当前 `GET /api/assets/` 只返回基础信息。
为了让“资产列表页”直接展示涨跌幅，建议 **增加可选参数** 控制是否附带行情：

* **接口（不变）**：`GET /api/assets/`
* **新增 Query 参数**：`withQuote=1`（默认 0）
* **响应 items 新增字段（当 withQuote=1）**

  * `price`, `changePct`, `quoteTime`（来自快照/缓存）

---

### 2.2 改动：资产详情接口附带最新行情（推荐）

你当前 `GET /api/assets/{asset_id}/` 仅基础信息。
建议：资产详情接口 **直接返回 quote 段**，减少前端额外请求（也可保持独立，看你架构偏好）。

* **接口（不变）**：`GET /api/assets/{asset_id}/`
* **响应新增**：`quote`（同 `/quote/` 数据结构的子集）

---

### 2.3 改动：创建帖子接口支持 assetCodes（关键：用户分享“代码”而不是“assetId”）

你现有 `POST /api/posts/` 支持 `assetIds`。
为了让用户可以直接输入 `000001`、`161725` 这类真实 code，建议兼容：

* **接口（不变）**：`POST /api/posts/`
* **请求体新增字段（二选一/可共存）**

  * `assetIds: number[]`（保留原有）
  * `assetCodes: string[]`（新增）
* **后端规则建议**

  1. 若同时传 `assetIds` 与 `assetCodes`：合并去重
  2. `assetCodes` 不存在时：返回 400（严格模式）或创建“待补全资产”（你已做资产库同步则可选）

**请求示例**

```json
{
  "title": "我看好平安银行",
  "content": "理由如下...",
  "tags": ["银行", "价值投资"],
  "status": "PENDING_REVIEW",
  "assetCodes": ["000001"]
}
```

---

### 2.4 改动：帖子详情/列表的 assets 字段补充 market / asset_type 命名统一

你现有帖子列表里 assets 元素字段是 `type`，资产表里是 `asset_type`。
建议统一为一个（例如 `assetType`），并补 `market` 便于前端展示：

* `assets[].assetType`
* `assets[].market`

---

### 2.5 （可选）改动：资产讨论区接口命名更通用（posts -> contents）

你现在是：`GET /api/assets/{asset_id}/posts/`。
如果你的系统未来不仅帖子，还会有“长文/研报/组合解读”等内容形态，建议（可不改，论文里写“兼容扩展”即可）：

* **保留兼容**：`/posts/`
* **新增更通用**：`GET /api/assets/{asset_id}/contents/`

  * Query：`type=POST|ARTICLE`，`sort=new|hot`

---

## 3. DRF 路由落地建议（最少改动、最清晰的 URL 组织）

建议在你现有 `assets` 路由下新增 3 个 detail route + 1 个 collection route：

* `assets/{id}/quote/`（detail route）
* `assets/{id}/kline/`（detail route）
* `assets/{id}/intraday/`（detail route）
* `assets/quotes/`（collection action，批量 quote）

并对 `posts` 的 create serializer 增加 `assetCodes` 入参解析逻辑（保持 `assetIds` 向后兼容）。

---

## 4. 你最终需要补充到“接口文档.mdc”的章节结构建议

你可以在现有 **6. 资产管理 (Assets)** 下新增小节：

* 6.4 资产最新行情（`GET /api/assets/{asset_id}/quote/`）
* 6.5 资产K线数据（`GET /api/assets/{asset_id}/kline/`）
* 6.6 资产分时数据（`GET /api/assets/{asset_id}/intraday/`）
* 6.7 批量行情（`POST /api/assets/quotes/`）
* （可选）6.8 行情推送 SSE（`GET /api/assets/{asset_id}/quote/stream/`）

并在 **3.2 创建帖子** 的请求参数表中新增一行 `assetCodes`。

---

如果你希望我“直接帮你把这些内容写进 /mnt/data/接口文档.mdc 的对应位置（按你原文档风格、字段命名风格统一）”，我可以继续给你输出一份**可直接复制粘贴**的 Markdown 增量段落（含参数表 + 响应示例）。
