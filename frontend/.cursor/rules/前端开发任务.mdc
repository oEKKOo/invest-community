# 📋 前端开发任务清单（行情扩展迭代）

> **文档说明**：本文档基于项目扩展目标（`项目扩展.mdc`）与后端新增接口文档（`新增接口文档.mdc`），梳理前端下一步需要完成的所有开发任务，按模块优先级排列。
> **技术栈**：Vue 3 + Element Plus + Pinia + Vue Router + TypeScript
> **后端 Base URL**：`http://127.0.0.1:8000`

---

## 🔑 阅读前提

在开始任何任务前，请确保已阅读：

- `接口文档.mdc`：已有接口的完整规范（Auth / Users / Posts / Assets / Portfolios / Admin 等）
- `background/.cursor/rules/新增接口文档.mdc`：本次迭代新增/改动接口（行情、K线、榜单等）
- `.cursor/rules/项目扩展.mdc`：产品定位与信息架构设计

---

## 🗺️ 总体任务地图

| 优先级 | 模块 | 任务描述 | 依赖接口 |
|---|---|---|---|
| P0 | **行情基础层** | 封装行情 API、类型定义 | 新增接口 1~4 |
| P0 | **资产搜索升级** | 搜索结果带行情字段展示 | 改动接口 11 |
| P0 | **个股详情页** | 新建个股详情页（行情+K线+内容聚合） | 新增接口 1/2/3/6 |
| P1 | **资产列表页** | 新建/升级资产列表页，带涨跌幅列 | 改动接口 11 |
| P1 | **涨跌幅榜单** | 新建市场榜单组件/页面 | 新增接口 7 |
| P1 | **帖子发布升级** | 支持用代码（assetCodes）关联标的 | 改动接口 13 |
| P1 | **帖子列表/详情** | 统一 assetType / market 展示 | 改动接口 14 |
| P2 | **实时行情推送** | SSE 实时订阅行情更新 | 新增接口 5 |
| P2 | **管理员数据监控** | 数据任务状态面板 | 新增接口 8/9/10 |
| P2 | **个人主页完善** | 发布历史、点评时间线 | 已有接口 |
| P3 | **合规与免责声明** | 全站免责声明、数据来源标注 | 无 |

---

## 📦 一、基础层任务（P0 — 开始所有其他任务之前完成）

### 1.1 类型定义扩展

**文件路径**：`src/types/market.ts`（新建）

需要定义以下 TypeScript 接口类型：

```
AssetQuote          // 行情快照（price/change/changePct/open/high/low/prevClose/volume/quoteTime/isStale）
AssetKlineItem      // K线条目（time/open/high/low/close/volume）
AssetKlineData      // K线数据容器（assetId/interval/items）
IntradayItem        // 分时数据（time/price/avgPrice/volume）
MarketRankingItem   // 榜单条目（assetId/code/name/market/price/changePct/volume）
DataJobStatus       // 数据任务状态
BulkQuoteResult     // 批量行情结果
```

同时扩展已有 `Asset` 类型，增加以下可选字段：
```
finnhub_symbol      // Finnhub 标识
exchange            // 交易所
currency            // 币种
industry            // 行业
logo_url            // 公司 Logo
description         // 公司简介
market              // 市场（SH/SZ/HK/US）
quote               // 内嵌行情快照（可选）
```

---

### 1.2 行情 API 服务层封装

**文件路径**：`src/api/market.ts`（新建）

需要封装以下函数（对应新增接口文档中的各接口）：

| 函数名 | 对应接口 | 说明 |
|---|---|---|
| `getAssetQuote(assetId)` | `GET /api/assets/{id}/quote/` | 获取单资产最新行情 |
| `getAssetKline(assetId, params)` | `GET /api/assets/{id}/kline/` | 获取 K 线数据 |
| `getAssetIntraday(assetId, params)` | `GET /api/assets/{id}/intraday/` | 获取分时数据 |
| `getBulkQuotes(assetIds)` | `POST /api/assets/quotes/` | 批量获取行情 |
| `getAssetContents(assetId, params)` | `GET /api/assets/{id}/contents/` | 资产相关内容聚合 |
| `getMarketRankings(params)` | `GET /api/market/rankings/` | 涨跌幅榜单 |
| `getMarketStatus()` | `GET /api/market/status/` | 数据状态（管理员） |
| `getDataJobs(params)` | `GET /api/market/jobs/` | 任务日志（管理员） |
| `triggerDataJob(jobType)` | `POST /api/market/jobs/trigger/` | 手动触发任务（管理员） |

**批量行情缓存建议**：在 Pinia store 中做短期内存缓存（60 秒），避免同一页面多个组件重复请求相同 assetId 的行情。

---

### 1.3 行情 Pinia Store

**文件路径**：`src/stores/market.ts`（新建）

Store 需要管理以下状态：

- `quoteCache: Map<number, { data: AssetQuote; fetchedAt: number }>` — 行情缓存（60s 有效期）
- `rankingList: MarketRankingItem[]` — 榜单缓存
- `actions`：`fetchQuote`、`fetchBulkQuotes`、`fetchRankings`（含缓存逻辑）

---

### 1.4 通用行情展示组件

**文件路径**：`src/components/market/`（新建目录）

| 组件名 | 说明 |
|---|---|
| `QuoteTag.vue` | 行情徽章：显示价格 + 涨跌幅，自动绿涨红跌配色 |
| `QuoteMini.vue` | 迷你行情卡：用于帖子/列表的行情内嵌（价格、涨跌、更新时间） |
| `AssetChip.vue` | 升级后的资产标签组件：显示代码 + 名称 + 行情徽章，可跳转个股详情 |

**颜色规范**：
- 上涨（changePct > 0）：`#f56c6c`（红色，A 股惯例）
- 下跌（changePct < 0）：`#67c23a`（绿色）
- 平盘（changePct = 0）：`#909399`（灰色）

---

## 🏪 二、个股详情页（P0 — 核心新功能）

### 2.1 路由注册

**文件路径**：`src/router/index.ts`

新增路由：
```
/assets/:assetId        → views/AssetDetail.vue       个股详情页
/market                 → views/MarketList.vue         行情列表页
/market/rankings        → views/MarketRankings.vue     榜单页（可与 MarketList 合并为 Tab）
```

---

### 2.2 个股详情页（主页面）

**文件路径**：`src/views/AssetDetail.vue`

**页面结构（6 个区块，按优先级实现）**：

#### 区块 1 — 标的头部卡片（P0 必须实现）

| 字段 | 数据来源 |
|---|---|
| 资产代码 + 名称 | `GET /api/assets/{id}/` |
| 市场标签（A股/港股/美股） | 资产详情的 `market` 字段 |
| 最新价 | `GET /api/assets/{id}/quote/` → `price` |
| 涨跌额 + 涨跌幅 | `change` + `changePct` |
| 开高低昨收 | `open/high/low/prevClose` |
| 成交量 | `volume` |
| 行情更新时间 | `quoteTime`，`isStale=true` 时显示"数据可能不是最新" |
| 数据来源标注 | 固定文案："行情数据来源：Finnhub，仅供学习参考" |

#### 区块 2 — K 线图（P0 必须实现）

- 使用 **ECharts**（或 Apache ECharts Vue 封装）渲染 K 线图
- 接口：`GET /api/assets/{id}/kline/?interval=1d&limit=200`
- 支持周期切换 Tab：`1d`（日K）/ `60m`（时K，若数据源支持）
- 图表下方显示成交量柱状图
- 加载状态：骨架屏或 Loading 遮罩

**组件文件**：`src/components/market/KlineChart.vue`

接收 props：
```
assetId: number
interval: '1d' | '60m' | '15m'（默认 1d）
limit: number（默认 200）
```

#### 区块 3 — 分时图（P1 可选）

- 接口：`GET /api/assets/{id}/intraday/`
- 展示当日价格走势折线图
- 与 K 线图共用组件区域，通过 Tab 切换（"日K" vs "分时"）

**组件文件**：`src/components/market/IntradayChart.vue`

#### 区块 4 — 核心指标（P1 轻量版）

- 数据来自 `GET /api/assets/{id}/quote/` 的字段
- 展示：52 周高（`high`）、52 周低（`low`）、成交量
- 若资产有 `industry`、`description` 字段，展示公司简介折叠区域

#### 区块 5 — 相关内容聚合（P0 必须实现）

- 接口：`GET /api/assets/{id}/contents/?sort=new&limit=10`
- 以时间线列表形式展示该标的相关帖子
- 每条展示：标题、作者、点赞数、时间
- 底部"查看更多"跳转到全部相关内容列表（或展开分页）

**复用现有帖子卡片组件**，无需新建。

#### 区块 6 — 讨论区快速发帖（P1 可选）

- 登录用户可在个股详情页底部快速发帖，自动关联当前标的
- 发帖请求体：`POST /api/posts/`，自动带入 `assetCodes: [asset.code]`

---

### 2.3 个股详情页 SEO/分享优化（P2 可选）

- 动态设置页面 `<title>` 为"股票代码 - 股票名称 - 投研社区"
- Open Graph meta 标签（用于分享链接预览）

---

## 📊 三、资产列表页升级（P1）

### 3.1 资产列表页

**文件路径**：`src/views/MarketList.vue`（新建或升级现有 Assets 列表）

**页面功能**：

| 功能 | 说明 |
|---|---|
| 搜索框 | 支持按代码/名称模糊搜索，调用 `GET /api/assets/?q=&withQuote=1` |
| 市场筛选 Tab | 全部 / A股 / 港股 / 美股（通过 `market` 参数筛选） |
| 资产类型筛选 | 股票 / 基金 / ETF |
| 列表展示 | 代码、名称、最新价、涨跌幅、成交量、所属市场 |
| 行情字段 | 当 `withQuote=1` 时，直接从列表接口获取行情，无需二次请求 |
| 点击进入 | 点击任意资产行 → 跳转 `/assets/:assetId` |
| 批量行情兜底 | 若列表接口未返回行情（`withQuote` 未开启），调用 `POST /api/assets/quotes/` 批量补充 |

**列表列配置**：
```
代码（code）| 名称（name）| 市场标签 | 最新价 | 涨跌幅（带颜色）| 成交量 | 操作
```

---

### 3.2 涨跌幅榜单

**文件路径**：`src/views/MarketRankings.vue` 或作为 `MarketList.vue` 中的 Tab

- 接口：`GET /api/market/rankings/?market=&assetType=&rankBy=change_pct&limit=50`
- 展示涨幅榜 / 跌幅榜 / 成交量榜（Tab 切换）
- 排行榜条目与资产列表样式一致，可复用列表组件

---

## ✏️ 四、帖子发布页升级（P1）

### 4.1 AssetSelect 组件升级

**文件路径**：`src/components/post/AssetSelect.vue`（升级）

当前功能：搜索返回 `assetId`，发帖时传 `assetIds[]`

**需要升级的点**：
- 搜索时调用 `GET /api/assets/?withQuote=1`，在下拉选项中展示行情徽章（价格 + 涨跌幅）
- 选中的资产以 `AssetChip.vue` 方式展示在输入框下方
- 发帖时，将选中资产的 `code` 拼成 `assetCodes[]` 字段传给后端（保留 `assetIds[]` 兼容）

**发帖请求体变化**：
```json
{
  "title": "...",
  "content": "...",
  "tags": [],
  "status": "PENDING_REVIEW",
  "assetCodes": ["000001", "AAPL"]
}
```

---

### 4.2 帖子列表/详情资产标签展示

**文件路径**：帖子卡片组件和详情页

- 帖子 `assets[]` 字段中统一使用 `assetType`、`market` 字段（已由后端统一）
- 每个资产标签渲染为 `AssetChip.vue`，显示代码 + 名称，点击跳转个股详情页
- 资产标签可选展示迷你行情（调用行情 store 的缓存数据，避免额外请求）

---

## 📡 五、SSE 实时行情推送（P2）

### 5.1 SSE 封装

**文件路径**：`src/composables/useQuoteStream.ts`（新建）

封装 `EventSource` 连接逻辑：

- 接口：`GET /api/assets/{id}/quote/stream/`
- 接收服务端推送的行情事件
- 自动更新 Pinia market store 中的行情缓存
- 页面卸载时自动关闭连接（`onUnmounted` cleanup）
- 断线重连（指数退避）

**用法建议**：仅在个股详情页开启 SSE（避免全站连接过多），列表页使用轮询（每 60s 调一次 `getAssetQuote`）。

---

### 5.2 个股详情页集成 SSE

- 进入 `/assets/:assetId` 页面时，启动 `useQuoteStream(assetId)`
- 行情数据实时更新头部卡片的价格、涨跌幅
- `isStale=false` 时不显示"数据可能不是最新"提示

---

## 🔧 六、管理员数据监控面板（P2）

**文件路径**：`src/views/admin/DataMonitor.vue`（新建）

**功能需求**：

### 6.1 数据状态概览

- 接口：`GET /api/market/status/`（需管理员权限）
- 展示：
  - 资产总数（按市场分组）
  - 最新快照记录数 / 最近更新时间
  - K 线数据条数

### 6.2 任务日志列表

- 接口：`GET /api/market/jobs/?page=1&pageSize=20`
- 以 Element Plus Table 展示任务日志
- 列：任务类型、执行时间、执行状态（成功/失败）、耗时、影响条数、错误信息
- 失败的任务用红色高亮

### 6.3 手动触发任务

- 接口：`POST /api/market/jobs/trigger/`
- 提供触发按钮：「同步股票列表」、「同步日 K 线」、「数据质量检查」、「清理旧快照」
- 触发后轮询任务状态（每 5 秒刷新一次列表，直到状态变更）

### 6.4 路由注册

- 路由：`/admin/data-monitor`
- 需要管理员权限守卫（已有 `role === 'ADMIN'` 判断逻辑）

---

## 👤 七、个人主页完善（P2）

### 7.1 作者主页

**文件路径**：`src/views/UserProfile.vue`（升级）

**新增内容**：
- "他发布的帖子"Tab → 调用 `GET /api/posts/?authorId={userId}`
- 帖子卡片内带关联标的的 `AssetChip`（行情可选显示）
- 关注/取关按钮（调用已有 `POST/DELETE /api/users/{userId}/follow/`）
- 展示"粉丝数 / 关注数"

### 7.2 我的主页

**文件路径**：`src/views/MyProfile.vue`（升级）

**新增/完善 Tab**：

| Tab | 接口 |
|---|---|
| 我的帖子 | `GET /api/posts/?authorId=me` |
| 我的收藏 | `GET /api/users/me/favorites/` |
| 我的组合 | `GET /api/portfolios/?userId=me` |
| 我的举报 | `GET /api/users/me/reports/` |

---

## ⚖️ 八、合规与免责声明（P3）

### 8.1 全站免责声明

- 在页脚固定展示：「本站数据仅供学习参考，不构成投资建议，请自行承担投资决策风险。」
- 数据来源标注：「行情数据来源：Finnhub Finance，更新频率约 60 秒」

### 8.2 发帖前弹窗提示

- 用户发布帖子/点评时，若关联了标的，弹出一次性确认弹窗：
  「您即将发布涉及金融标的的内容，请确保内容仅供交流，不构成投资建议，如有误导请联系管理员。」
- 确认后本地存储标记，不再重复弹出（`localStorage`）

### 8.3 个股详情页数据提示

- 在 K 线图和行情卡片旁显示小文字：「数据来源：Finnhub | 更新时间：{quoteTime}」
- `isStale=true` 时显示黄色告警：「行情数据已超过 60 秒，可能不是最新」

---

## 🔁 九、已有功能 API 适配修复（P1）

以下是因后端接口字段变更，前端需要同步适配的地方：

| 位置 | 问题 | 修复方案 |
|---|---|---|
| 帖子列表/详情 `assets[]` | 原 `type` 字段改为 `assetType` | 全局替换引用 |
| 帖子列表/详情 `assets[]` | 新增 `market` 字段 | 在 `AssetChip` 中展示市场标签 |
| 资产详情 `GET /api/assets/{id}/` | 响应新增 `quote` 嵌套对象 | 个股详情页优先读 `quote`，无则单独请求 `/quote/` |
| 资产列表 `GET /api/assets/` | 支持 `withQuote=1` | 列表页请求时添加参数 |

---

## 📐 十、组件设计约定

### 颜色主题（行情专用）

```css
--color-up:   #f56c6c;   /* 上涨红 */
--color-down: #67c23a;   /* 下跌绿 */
--color-flat: #909399;   /* 平盘灰 */
```

### 数字格式化

| 场景 | 格式 |
|---|---|
| 价格 | 保留 2 位小数，如 `12.34` |
| 涨跌幅 | 保留 2 位小数 + `%`，正数显示 `+`，如 `+1.23%` |
| 成交量 | 以万/亿为单位，如 `1.23亿` |
| 时间 | 行情时间显示"MM-DD HH:mm"，其余显示"YYYY-MM-DD" |

### Loading 状态

- 所有行情接口请求期间显示骨架屏（El Skeleton）
- 行情请求失败（4040 / 网络错误）时展示"暂无行情数据"空态提示
- K 线图加载超过 3 秒显示"数据加载中..."文字提示

---

## 📅 建议实施顺序

```
Week 1：
  Day 1-2：任务 1（基础层：类型/API/Store/通用组件）
  Day 3-5：任务 2（个股详情页，完成区块 1、2、5）

Week 2：
  Day 1-2：任务 4（帖子发布升级 + 列表适配）
  Day 3-4：任务 3（资产列表页 + 榜单）
  Day 5：  任务 8（合规声明）

Week 3（增强）：
  任务 5（SSE 实时行情）
  任务 6（管理员监控面板）
  任务 7（个人主页完善）
  任务 2 区块 3/4/6 补全
```

---

## ✅ 自测验收清单

完成每个模块后，请按以下清单自测：

### 行情展示

- [ ] 个股详情页能正确显示最新价和涨跌幅
- [ ] 涨跌颜色正确（红涨绿跌）
- [ ] K 线图能正常渲染，数据点不乱序
- [ ] `isStale=true` 时有告警提示
- [ ] 行情接口失败时有降级提示（不白屏）

### 资产列表

- [ ] 搜索框输入后列表正确过滤
- [ ] 市场筛选 Tab 切换正常
- [ ] 点击资产行跳转到正确的个股详情页

### 帖子发布

- [ ] 选择标的时下拉显示行情信息
- [ ] 发帖请求体包含 `assetCodes` 字段
- [ ] 帖子列表中标的标签能跳转到个股详情页

### 管理员面板

- [ ] 非管理员用户无法访问 `/admin/data-monitor`
- [ ] 任务列表正确展示状态（成功/失败区分）
- [ ] 手动触发任务后列表刷新

---

*最后更新：2026-02-22*
