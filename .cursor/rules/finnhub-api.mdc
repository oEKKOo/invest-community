---
alwaysApply: false
description: Finnhub 第三方行情数据接入规范（Key 管理 / 调用约定 / 数据同步）
---

## Finnhub 接入规则（本项目）

### 1) 安全与密钥管理（必须遵守）

- **禁止**在仓库内以明文形式存放 Finnhub API Key（包括：规则文件、代码、README、截图、日志）。
- **禁止**在前端直接调用 Finnhub（会暴露 Key）。统一通过后端代理/聚合接口转发。
- **必须**使用环境变量读取 Key：
  - **环境变量名**：`FINNHUB_API_KEY`
  - **本地开发**：放在不入库的 `.env`（或系统环境变量）
  - **部署环境**：在服务器环境变量/密钥管理服务中配置
- **日志脱敏**：任何请求日志、报错日志里不得打印 Key；如必须记录配置状态，仅记录“是否已配置”。

> 说明：你已经在对话中提供过 Key，但为了避免误提交与泄露，本规则文件不会写入真实 Key。请在本机/服务器以环境变量方式配置。

---

### 2) 数据源定位与覆盖边界（先 A 股，后港美股）

- **项目阶段策略**
  - **阶段 1（优先 A 股）**：先验证 Finnhub 对上/深/北交所的覆盖情况是否满足你“股票列表 + 日线”的最小可用数据集（MVD）。
  - **阶段 2（港股/美股）**：Finnhub 通常覆盖更好，可作为港美股主数据源。
- **覆盖不足的处理原则**
  - 若 Finnhub 对 A 股标的/日线覆盖不足：保持接口与数据模型不变，**允许引入第二数据源作为 fallback**（仅替换采集层，不影响上层页面与社区内容）。

---

### 3) 统一标的标识规范（跨市场必须统一）

为支持“先 A 股、后港美股”的扩展，项目内对标的使用统一主键：

- **内部标识**：`market + ticker`
  - `market` 建议值：`CN`（A 股）、`HK`（港股）、`US`（美股）
  - `ticker`：交易所/供应商通用可读的代码串
- **供应商标识（Finnhub symbol）**：单独存储并可更新
  - 原则：**内部标识稳定**，供应商 symbol 可变（便于未来替换数据源）

---

### 4) Finnhub API 调用约定（接口契约层面）

- **Base URL**：`https://finnhub.io/api/v1`
- **鉴权方式**：通过查询参数 `token=$FINNHUB_API_KEY`（由后端在服务器端拼接）
- **错误处理（必须）**
  - **429 / rate limit**：指数退避重试 + 全局限流
  - **5xx**：重试（次数上限）+ 任务记录失败原因
  - **4xx**：一般不重试；记录请求参数与响应摘要（脱敏）
- **缓存建议（强烈建议）**
  - 个股详情的“最新行情/quote”：短缓存（例如 10~60 秒，视你需求）
  - 日线/历史 K 线：长缓存（分钟~小时级）或直接从数据库读

---

### 5) 数据同步任务（从“虚拟数据”过渡到“真实数据”的落地方式）

建议将数据同步拆为 3 类任务，并为每次运行记录 `DataJobLog`（开始/结束、成功失败、影响条数、失败原因）：

- **任务 A：标的清单同步（Symbols Sync）**
  - 目标：拉取/更新股票列表、名称、交易所、交易状态等主数据
  - 输出：更新 `Symbol` 主表 + “内部标识 ↔ Finnhub symbol”映射
- **任务 B：日线同步（Daily Candles Sync）**
  - 目标：按市场/标的增量拉取日线 OHLCV
  - 频率：交易日收盘后一次增量；必要时支持回补指定日期范围
- **任务 C：数据质量校验（DQ Check）**
  - 缺失检测：交易日是否缺 K 线
  - 重复检测：同一标的同一交易日重复记录
  - 异常检测：极端跳点（可选）——避免第三方异常导致图表失真

---

### 6) 对外分享型内容与数据联动（个股页必须做到的聚合规则）

你现有项目已支持 UGC 与“关联标的代码”，扩展时遵循：

- **个股详情页聚合**
  - 数据区：行情 + 指标（来自 Finnhub→入库→对外 API）
  - 内容区：该标的关联的帖子/文章/点评/策略（来自你现有 UGC）
- **内容发布规范**
  - 点评/策略发布时，必须选择或校验关联标的（避免“无效代码”导致无法聚合）
- **审核与治理**
  - 点评/策略建议“先审后发”，普通帖子可“先发后审/抽检”

---

### 7) 最小验证清单（接入 Finnhub 后必须手工验收）

- **Key 配置验收**
  - 服务启动后能识别 `FINNHUB_API_KEY` 已配置（仅显示布尔状态）
- **数据可用性验收**
  - 能拿到：股票列表（至少覆盖你想展示的市场）/ 单股 quote / 单股日线 candles
  - 个股页：图表连续、无重复、更新时间可展示
- **限流与稳定性验收**
  - 压测/批量同步时不触发大量 429；触发时能自动退避并最终完成或记录失败

